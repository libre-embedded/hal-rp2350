/**
 * \file
 * \brief Generated by ifgen (4.7.8).
 */

#pragma once
#ifndef RP2350_STRUCTS_TBMAN_H
#define RP2350_STRUCTS_TBMAN_H

#include "../ifgen/common.h"

namespace RP2350
{

/**
 * For managing simulation testbenches
 */
struct tbman
{
    /* Constant attributes. */
    static constexpr struct_id_t id = 38;  /*!< tbman's identifier. */
    static constexpr std::size_t size = 4; /*!< tbman's size in bytes. */

    /* Fields. */
    const uint32_t PLATFORM =
        {}; /*!< (read-only) Indicates the type of platform in use */

    /* Methods. */

    /**
     * Get PLATFORM's ASIC bit.
     *
     * Indicates the platform is an ASIC
     */
    inline bool get_PLATFORM_ASIC() volatile
    {
        return PLATFORM & 1u;
    }

    /**
     * Get PLATFORM's FPGA bit.
     *
     * Indicates the platform is an FPGA
     */
    inline bool get_PLATFORM_FPGA() volatile
    {
        return PLATFORM & (1u << 1u);
    }

    /**
     * Get PLATFORM's HDLSIM bit.
     *
     * Indicates the platform is a simulation
     */
    inline bool get_PLATFORM_HDLSIM() volatile
    {
        return PLATFORM & (1u << 2u);
    }

    /**
     * Get all of PLATFORM's bit fields.
     *
     * (read-only) Indicates the type of platform in use
     */
    inline void get_PLATFORM(bool &ASIC, bool &FPGA, bool &HDLSIM) volatile
    {
        uint32_t curr = PLATFORM;

        ASIC = curr & 1u;
        FPGA = curr & (1u << 1u);
        HDLSIM = curr & (1u << 2u);
    }
};

static_assert(sizeof(tbman) == tbman::size);
static_assert(ifgen_struct<tbman>);

static volatile tbman *const TBMAN = reinterpret_cast<tbman *>(0x40160000);

}; // namespace RP2350

#endif
