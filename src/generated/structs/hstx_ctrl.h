/**
 * \file
 * \brief Generated by ifgen (4.7.1).
 */

#pragma once
#ifndef RP2350_STRUCTS_HSTX_CTRL_H
#define RP2350_STRUCTS_HSTX_CTRL_H

#include "../ifgen/common.h"

namespace RP2350
{

/**
 * Control interface to HSTX. For FIFO write access and status, see the
 * HSTX_FIFO register block.
 */
struct hstx_ctrl
{
    /* Constant attributes. */
    static constexpr struct_id_t id = 10;   /*!< hstx_ctrl's identifier. */
    static constexpr std::size_t size = 44; /*!< hstx_ctrl's size in bytes. */

    /* Fields. */
    uint32_t CSR;
    uint32_t BIT0; /*!< (read-write) Data control register for output bit 0 */
    uint32_t BIT1; /*!< (read-write) Data control register for output bit 1 */
    uint32_t BIT2; /*!< (read-write) Data control register for output bit 2 */
    uint32_t BIT3; /*!< (read-write) Data control register for output bit 3 */
    uint32_t BIT4; /*!< (read-write) Data control register for output bit 4 */
    uint32_t BIT5; /*!< (read-write) Data control register for output bit 5 */
    uint32_t BIT6; /*!< (read-write) Data control register for output bit 6 */
    uint32_t BIT7; /*!< (read-write) Data control register for output bit 7 */
    uint32_t EXPAND_SHIFT; /*!< (read-write) Configure the optional shifter
                              inside the command expander */
    uint32_t EXPAND_TMDS; /*!< (read-write) Configure the optional TMDS encoder
                             inside the command expander */

    /* Methods. */

    /**
     * Get CSR's EN bit.
     *
     * When EN is 1, the HSTX will shift out data as it appears in the FIFO. As
     * long as there is data, the HSTX shift register will shift once per clock
     * cycle, and the frequency of popping from the FIFO is determined by the
     * ratio of SHIFT and SHIFT_THRESH.
     *
     *                             When EN is 0, the FIFO is not popped. The
     * shift counter and clock generator are also reset to their initial state
     * for as long as EN is low. Note the initial phase of the clock generator
     * can be configured by the CLKPHASE field.
     *
     *                             Once the HSTX is enabled again, and data is
     * pushed to the FIFO, the generated clock's first rising edge will be one
     * half-period after the first data is launched.
     */
    inline bool get_CSR_EN() volatile
    {
        return CSR & 1u;
    }

    /**
     * Set CSR's EN bit.
     *
     * When EN is 1, the HSTX will shift out data as it appears in the FIFO. As
     * long as there is data, the HSTX shift register will shift once per clock
     * cycle, and the frequency of popping from the FIFO is determined by the
     * ratio of SHIFT and SHIFT_THRESH.
     *
     *                             When EN is 0, the FIFO is not popped. The
     * shift counter and clock generator are also reset to their initial state
     * for as long as EN is low. Note the initial phase of the clock generator
     * can be configured by the CLKPHASE field.
     *
     *                             Once the HSTX is enabled again, and data is
     * pushed to the FIFO, the generated clock's first rising edge will be one
     * half-period after the first data is launched.
     */
    inline void set_CSR_EN() volatile
    {
        CSR |= 1u;
    }

    /**
     * Clear CSR's EN bit.
     *
     * When EN is 1, the HSTX will shift out data as it appears in the FIFO. As
     * long as there is data, the HSTX shift register will shift once per clock
     * cycle, and the frequency of popping from the FIFO is determined by the
     * ratio of SHIFT and SHIFT_THRESH.
     *
     *                             When EN is 0, the FIFO is not popped. The
     * shift counter and clock generator are also reset to their initial state
     * for as long as EN is low. Note the initial phase of the clock generator
     * can be configured by the CLKPHASE field.
     *
     *                             Once the HSTX is enabled again, and data is
     * pushed to the FIFO, the generated clock's first rising edge will be one
     * half-period after the first data is launched.
     */
    inline void clear_CSR_EN() volatile
    {
        CSR &= ~(1u);
    }

    /**
     * Toggle CSR's EN bit.
     *
     * When EN is 1, the HSTX will shift out data as it appears in the FIFO. As
     * long as there is data, the HSTX shift register will shift once per clock
     * cycle, and the frequency of popping from the FIFO is determined by the
     * ratio of SHIFT and SHIFT_THRESH.
     *
     *                             When EN is 0, the FIFO is not popped. The
     * shift counter and clock generator are also reset to their initial state
     * for as long as EN is low. Note the initial phase of the clock generator
     * can be configured by the CLKPHASE field.
     *
     *                             Once the HSTX is enabled again, and data is
     * pushed to the FIFO, the generated clock's first rising edge will be one
     * half-period after the first data is launched.
     */
    inline void toggle_CSR_EN() volatile
    {
        CSR ^= 1u;
    }

    /**
     * Get CSR's EXPAND_EN bit.
     *
     * Enable the command expander. When 0, raw FIFO data is passed directly to
     * the output shift register. When 1, the command expander can perform
     * simple operations such as run length decoding on data between the FIFO
     * and the shift register.
     *
     *                             Do not change CXPD_EN whilst EN is set. It's
     * safe to set CXPD_EN simultaneously with setting EN.
     */
    inline bool get_CSR_EXPAND_EN() volatile
    {
        return CSR & (1u << 1u);
    }

    /**
     * Set CSR's EXPAND_EN bit.
     *
     * Enable the command expander. When 0, raw FIFO data is passed directly to
     * the output shift register. When 1, the command expander can perform
     * simple operations such as run length decoding on data between the FIFO
     * and the shift register.
     *
     *                             Do not change CXPD_EN whilst EN is set. It's
     * safe to set CXPD_EN simultaneously with setting EN.
     */
    inline void set_CSR_EXPAND_EN() volatile
    {
        CSR |= 1u << 1u;
    }

    /**
     * Clear CSR's EXPAND_EN bit.
     *
     * Enable the command expander. When 0, raw FIFO data is passed directly to
     * the output shift register. When 1, the command expander can perform
     * simple operations such as run length decoding on data between the FIFO
     * and the shift register.
     *
     *                             Do not change CXPD_EN whilst EN is set. It's
     * safe to set CXPD_EN simultaneously with setting EN.
     */
    inline void clear_CSR_EXPAND_EN() volatile
    {
        CSR &= ~(1u << 1u);
    }

    /**
     * Toggle CSR's EXPAND_EN bit.
     *
     * Enable the command expander. When 0, raw FIFO data is passed directly to
     * the output shift register. When 1, the command expander can perform
     * simple operations such as run length decoding on data between the FIFO
     * and the shift register.
     *
     *                             Do not change CXPD_EN whilst EN is set. It's
     * safe to set CXPD_EN simultaneously with setting EN.
     */
    inline void toggle_CSR_EXPAND_EN() volatile
    {
        CSR ^= 1u << 1u;
    }

    /**
     * Get CSR's COUPLED_MODE bit.
     *
     * Enable the PIO-to-HSTX 1:1 connection. The HSTX must be clocked
     * *directly* from the system clock (not just from some other clock source
     * of the same frequency) for this synchronous interface to function
     * correctly.
     *
     *                             When COUPLED_MODE is set, BITx_SEL_P and
     * SEL_N indices 24 through 31 will select bits from the 8-bit PIO-to-HSTX
     * path, rather than shifter bits. Indices of 0 through 23 will still index
     * the shift register as normal.
     *
     *                             The PIO outputs connected to the PIO-to-HSTX
     * bus are those same outputs that would appear on the HSTX-capable pins if
     * those pins' FUNCSELs were set to PIO instead of HSTX.
     *
     *                             For example, if HSTX is on GPIOs 12 through
     * 19, then PIO outputs 12 through 19 are connected to the HSTX when
     * coupled mode is engaged.
     */
    inline bool get_CSR_COUPLED_MODE() volatile
    {
        return CSR & (1u << 4u);
    }

    /**
     * Set CSR's COUPLED_MODE bit.
     *
     * Enable the PIO-to-HSTX 1:1 connection. The HSTX must be clocked
     * *directly* from the system clock (not just from some other clock source
     * of the same frequency) for this synchronous interface to function
     * correctly.
     *
     *                             When COUPLED_MODE is set, BITx_SEL_P and
     * SEL_N indices 24 through 31 will select bits from the 8-bit PIO-to-HSTX
     * path, rather than shifter bits. Indices of 0 through 23 will still index
     * the shift register as normal.
     *
     *                             The PIO outputs connected to the PIO-to-HSTX
     * bus are those same outputs that would appear on the HSTX-capable pins if
     * those pins' FUNCSELs were set to PIO instead of HSTX.
     *
     *                             For example, if HSTX is on GPIOs 12 through
     * 19, then PIO outputs 12 through 19 are connected to the HSTX when
     * coupled mode is engaged.
     */
    inline void set_CSR_COUPLED_MODE() volatile
    {
        CSR |= 1u << 4u;
    }

    /**
     * Clear CSR's COUPLED_MODE bit.
     *
     * Enable the PIO-to-HSTX 1:1 connection. The HSTX must be clocked
     * *directly* from the system clock (not just from some other clock source
     * of the same frequency) for this synchronous interface to function
     * correctly.
     *
     *                             When COUPLED_MODE is set, BITx_SEL_P and
     * SEL_N indices 24 through 31 will select bits from the 8-bit PIO-to-HSTX
     * path, rather than shifter bits. Indices of 0 through 23 will still index
     * the shift register as normal.
     *
     *                             The PIO outputs connected to the PIO-to-HSTX
     * bus are those same outputs that would appear on the HSTX-capable pins if
     * those pins' FUNCSELs were set to PIO instead of HSTX.
     *
     *                             For example, if HSTX is on GPIOs 12 through
     * 19, then PIO outputs 12 through 19 are connected to the HSTX when
     * coupled mode is engaged.
     */
    inline void clear_CSR_COUPLED_MODE() volatile
    {
        CSR &= ~(1u << 4u);
    }

    /**
     * Toggle CSR's COUPLED_MODE bit.
     *
     * Enable the PIO-to-HSTX 1:1 connection. The HSTX must be clocked
     * *directly* from the system clock (not just from some other clock source
     * of the same frequency) for this synchronous interface to function
     * correctly.
     *
     *                             When COUPLED_MODE is set, BITx_SEL_P and
     * SEL_N indices 24 through 31 will select bits from the 8-bit PIO-to-HSTX
     * path, rather than shifter bits. Indices of 0 through 23 will still index
     * the shift register as normal.
     *
     *                             The PIO outputs connected to the PIO-to-HSTX
     * bus are those same outputs that would appear on the HSTX-capable pins if
     * those pins' FUNCSELs were set to PIO instead of HSTX.
     *
     *                             For example, if HSTX is on GPIOs 12 through
     * 19, then PIO outputs 12 through 19 are connected to the HSTX when
     * coupled mode is engaged.
     */
    inline void toggle_CSR_COUPLED_MODE() volatile
    {
        CSR ^= 1u << 4u;
    }

    /**
     * Get CSR's COUPLED_SEL field.
     *
     * Select which PIO to use for coupled mode operation.
     */
    inline uint8_t get_CSR_COUPLED_SEL() volatile
    {
        return (CSR >> 5u) & 0b11u;
    }

    /**
     * Set CSR's COUPLED_SEL field.
     *
     * Select which PIO to use for coupled mode operation.
     */
    inline void set_CSR_COUPLED_SEL(uint8_t value) volatile
    {
        uint32_t curr = CSR;

        curr &= ~(0b11u << 5u);
        curr |= (value & 0b11u) << 5u;

        CSR = curr;
    }

    /**
     * Get CSR's SHIFT field.
     *
     * How many bits to right-rotate the shift register by each cycle.
     *
     *                             The use of a rotate rather than a shift
     * allows left shifts to be emulated, by subtracting the left-shift amount
     * from 32. It also allows data to be repeated, when the product of SHIFT
     * and N_SHIFTS is greater than 32.
     */
    inline uint8_t get_CSR_SHIFT() volatile
    {
        return (CSR >> 8u) & 0b11111u;
    }

    /**
     * Set CSR's SHIFT field.
     *
     * How many bits to right-rotate the shift register by each cycle.
     *
     *                             The use of a rotate rather than a shift
     * allows left shifts to be emulated, by subtracting the left-shift amount
     * from 32. It also allows data to be repeated, when the product of SHIFT
     * and N_SHIFTS is greater than 32.
     */
    inline void set_CSR_SHIFT(uint8_t value) volatile
    {
        uint32_t curr = CSR;

        curr &= ~(0b11111u << 8u);
        curr |= (value & 0b11111u) << 8u;

        CSR = curr;
    }

    /**
     * Get CSR's N_SHIFTS field.
     *
     * Number of times to shift the shift register before refilling it from the
     * FIFO. (A count of how many times it has been shifted, *not* the total
     * shift distance.)
     *
     *                             A register value of 0 means shift 32 times.
     */
    inline uint8_t get_CSR_N_SHIFTS() volatile
    {
        return (CSR >> 16u) & 0b11111u;
    }

    /**
     * Set CSR's N_SHIFTS field.
     *
     * Number of times to shift the shift register before refilling it from the
     * FIFO. (A count of how many times it has been shifted, *not* the total
     * shift distance.)
     *
     *                             A register value of 0 means shift 32 times.
     */
    inline void set_CSR_N_SHIFTS(uint8_t value) volatile
    {
        uint32_t curr = CSR;

        curr &= ~(0b11111u << 16u);
        curr |= (value & 0b11111u) << 16u;

        CSR = curr;
    }

    /**
     * Get CSR's CLKPHASE field.
     *
     * Set the initial phase of the generated clock.
     *
     *                             A CLKPHASE of 0 means the clock is initially
     * low, and the first rising edge occurs after one half period of the
     * generated clock (i.e. CLKDIV/2 cycles of clk_hstx). Incrementing
     * CLKPHASE by 1 will advance the initial clock phase by one half clk_hstx
     * period. For example, if CLKDIV=2 and CLKPHASE=1:
     *
     *                             * The clock will be initially low
     *
     *                             * The first rising edge will be 0.5 clk_hstx
     * cycles after asserting first data
     *
     *                             * The first falling edge will be 1.5
     * clk_hstx cycles after asserting first data
     *
     *                             This configuration would be suitable for
     * serialising at a bit rate of clk_hstx with a centre-aligned DDR clock.
     *
     *                             When the HSTX is halted by clearing CSR_EN,
     * the clock generator will return to its initial phase as configured by
     * the CLKPHASE field.
     *
     *                             Note CLKPHASE must be strictly less than
     * double the value of CLKDIV (one full period), else its operation is
     * undefined.
     */
    inline uint8_t get_CSR_CLKPHASE() volatile
    {
        return (CSR >> 24u) & 0xfu;
    }

    /**
     * Set CSR's CLKPHASE field.
     *
     * Set the initial phase of the generated clock.
     *
     *                             A CLKPHASE of 0 means the clock is initially
     * low, and the first rising edge occurs after one half period of the
     * generated clock (i.e. CLKDIV/2 cycles of clk_hstx). Incrementing
     * CLKPHASE by 1 will advance the initial clock phase by one half clk_hstx
     * period. For example, if CLKDIV=2 and CLKPHASE=1:
     *
     *                             * The clock will be initially low
     *
     *                             * The first rising edge will be 0.5 clk_hstx
     * cycles after asserting first data
     *
     *                             * The first falling edge will be 1.5
     * clk_hstx cycles after asserting first data
     *
     *                             This configuration would be suitable for
     * serialising at a bit rate of clk_hstx with a centre-aligned DDR clock.
     *
     *                             When the HSTX is halted by clearing CSR_EN,
     * the clock generator will return to its initial phase as configured by
     * the CLKPHASE field.
     *
     *                             Note CLKPHASE must be strictly less than
     * double the value of CLKDIV (one full period), else its operation is
     * undefined.
     */
    inline void set_CSR_CLKPHASE(uint8_t value) volatile
    {
        uint32_t curr = CSR;

        curr &= ~(0xfu << 24u);
        curr |= (value & 0xfu) << 24u;

        CSR = curr;
    }

    /**
     * Get CSR's CLKDIV field.
     *
     * Clock period of the generated clock, measured in HSTX clock cycles. Can
     * be odd or even. The generated clock advances only on cycles where the
     * shift register shifts.
     *
     *                             For example, a clkdiv of 5 would generate a
     * complete output clock period for every 5 HSTX clocks (or every 10
     * half-clocks).
     *
     *                             A CLKDIV value of 0 is mapped to a period of
     * 16 HSTX clock cycles.
     */
    inline uint8_t get_CSR_CLKDIV() volatile
    {
        return (CSR >> 28u) & 0xfu;
    }

    /**
     * Set CSR's CLKDIV field.
     *
     * Clock period of the generated clock, measured in HSTX clock cycles. Can
     * be odd or even. The generated clock advances only on cycles where the
     * shift register shifts.
     *
     *                             For example, a clkdiv of 5 would generate a
     * complete output clock period for every 5 HSTX clocks (or every 10
     * half-clocks).
     *
     *                             A CLKDIV value of 0 is mapped to a period of
     * 16 HSTX clock cycles.
     */
    inline void set_CSR_CLKDIV(uint8_t value) volatile
    {
        uint32_t curr = CSR;

        curr &= ~(0xfu << 28u);
        curr |= (value & 0xfu) << 28u;

        CSR = curr;
    }

    /**
     * Get all of CSR's bit fields.
     */
    inline void get_CSR(bool &EN, bool &EXPAND_EN, bool &COUPLED_MODE,
                        uint8_t &COUPLED_SEL, uint8_t &SHIFT,
                        uint8_t &N_SHIFTS, uint8_t &CLKPHASE,
                        uint8_t &CLKDIV) volatile
    {
        uint32_t curr = CSR;

        EN = curr & 1u;
        EXPAND_EN = curr & (1u << 1u);
        COUPLED_MODE = curr & (1u << 4u);
        COUPLED_SEL = (curr >> 5u) & 0b11u;
        SHIFT = (curr >> 8u) & 0b11111u;
        N_SHIFTS = (curr >> 16u) & 0b11111u;
        CLKPHASE = (curr >> 24u) & 0xfu;
        CLKDIV = (curr >> 28u) & 0xfu;
    }

    /**
     * Set all of CSR's bit fields.
     */
    inline void set_CSR(bool EN, bool EXPAND_EN, bool COUPLED_MODE,
                        uint8_t COUPLED_SEL, uint8_t SHIFT, uint8_t N_SHIFTS,
                        uint8_t CLKPHASE, uint8_t CLKDIV) volatile
    {
        uint32_t curr = CSR;

        curr &= ~(0b1u);
        curr |= (EN & 0b1u);
        curr &= ~(0b1u << 1u);
        curr |= (EXPAND_EN & 0b1u) << 1u;
        curr &= ~(0b1u << 4u);
        curr |= (COUPLED_MODE & 0b1u) << 4u;
        curr &= ~(0b11u << 5u);
        curr |= (COUPLED_SEL & 0b11u) << 5u;
        curr &= ~(0b11111u << 8u);
        curr |= (SHIFT & 0b11111u) << 8u;
        curr &= ~(0b11111u << 16u);
        curr |= (N_SHIFTS & 0b11111u) << 16u;
        curr &= ~(0xfu << 24u);
        curr |= (CLKPHASE & 0xfu) << 24u;
        curr &= ~(0xfu << 28u);
        curr |= (CLKDIV & 0xfu) << 28u;

        CSR = curr;
    }

    /**
     * Get BIT0's SEL_P field.
     *
     * Shift register data bit select for the first half of the HSTX clock
     * cycle
     */
    inline uint8_t get_BIT0_SEL_P() volatile
    {
        return BIT0 & 0b11111u;
    }

    /**
     * Set BIT0's SEL_P field.
     *
     * Shift register data bit select for the first half of the HSTX clock
     * cycle
     */
    inline void set_BIT0_SEL_P(uint8_t value) volatile
    {
        uint32_t curr = BIT0;

        curr &= ~(0b11111u);
        curr |= (value & 0b11111u);

        BIT0 = curr;
    }

    /**
     * Get BIT0's SEL_N field.
     *
     * Shift register data bit select for the second half of the HSTX clock
     * cycle
     */
    inline uint8_t get_BIT0_SEL_N() volatile
    {
        return (BIT0 >> 8u) & 0b11111u;
    }

    /**
     * Set BIT0's SEL_N field.
     *
     * Shift register data bit select for the second half of the HSTX clock
     * cycle
     */
    inline void set_BIT0_SEL_N(uint8_t value) volatile
    {
        uint32_t curr = BIT0;

        curr &= ~(0b11111u << 8u);
        curr |= (value & 0b11111u) << 8u;

        BIT0 = curr;
    }

    /**
     * Get BIT0's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline bool get_BIT0_INV() volatile
    {
        return BIT0 & (1u << 16u);
    }

    /**
     * Set BIT0's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void set_BIT0_INV() volatile
    {
        BIT0 |= 1u << 16u;
    }

    /**
     * Clear BIT0's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void clear_BIT0_INV() volatile
    {
        BIT0 &= ~(1u << 16u);
    }

    /**
     * Toggle BIT0's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void toggle_BIT0_INV() volatile
    {
        BIT0 ^= 1u << 16u;
    }

    /**
     * Get BIT0's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline bool get_BIT0_CLK() volatile
    {
        return BIT0 & (1u << 17u);
    }

    /**
     * Set BIT0's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void set_BIT0_CLK() volatile
    {
        BIT0 |= 1u << 17u;
    }

    /**
     * Clear BIT0's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void clear_BIT0_CLK() volatile
    {
        BIT0 &= ~(1u << 17u);
    }

    /**
     * Toggle BIT0's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void toggle_BIT0_CLK() volatile
    {
        BIT0 ^= 1u << 17u;
    }

    /**
     * Get all of BIT0's bit fields.
     *
     * (read-write) Data control register for output bit 0
     */
    inline void get_BIT0(uint8_t &SEL_P, uint8_t &SEL_N, bool &INV,
                         bool &CLK) volatile
    {
        uint32_t curr = BIT0;

        SEL_P = curr & 0b11111u;
        SEL_N = (curr >> 8u) & 0b11111u;
        INV = curr & (1u << 16u);
        CLK = curr & (1u << 17u);
    }

    /**
     * Set all of BIT0's bit fields.
     *
     * (read-write) Data control register for output bit 0
     */
    inline void set_BIT0(uint8_t SEL_P, uint8_t SEL_N, bool INV,
                         bool CLK) volatile
    {
        uint32_t curr = BIT0;

        curr &= ~(0b11111u);
        curr |= (SEL_P & 0b11111u);
        curr &= ~(0b11111u << 8u);
        curr |= (SEL_N & 0b11111u) << 8u;
        curr &= ~(0b1u << 16u);
        curr |= (INV & 0b1u) << 16u;
        curr &= ~(0b1u << 17u);
        curr |= (CLK & 0b1u) << 17u;

        BIT0 = curr;
    }

    /**
     * Get BIT1's SEL_P field.
     *
     * Shift register data bit select for the first half of the HSTX clock
     * cycle
     */
    inline uint8_t get_BIT1_SEL_P() volatile
    {
        return BIT1 & 0b11111u;
    }

    /**
     * Set BIT1's SEL_P field.
     *
     * Shift register data bit select for the first half of the HSTX clock
     * cycle
     */
    inline void set_BIT1_SEL_P(uint8_t value) volatile
    {
        uint32_t curr = BIT1;

        curr &= ~(0b11111u);
        curr |= (value & 0b11111u);

        BIT1 = curr;
    }

    /**
     * Get BIT1's SEL_N field.
     *
     * Shift register data bit select for the second half of the HSTX clock
     * cycle
     */
    inline uint8_t get_BIT1_SEL_N() volatile
    {
        return (BIT1 >> 8u) & 0b11111u;
    }

    /**
     * Set BIT1's SEL_N field.
     *
     * Shift register data bit select for the second half of the HSTX clock
     * cycle
     */
    inline void set_BIT1_SEL_N(uint8_t value) volatile
    {
        uint32_t curr = BIT1;

        curr &= ~(0b11111u << 8u);
        curr |= (value & 0b11111u) << 8u;

        BIT1 = curr;
    }

    /**
     * Get BIT1's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline bool get_BIT1_INV() volatile
    {
        return BIT1 & (1u << 16u);
    }

    /**
     * Set BIT1's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void set_BIT1_INV() volatile
    {
        BIT1 |= 1u << 16u;
    }

    /**
     * Clear BIT1's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void clear_BIT1_INV() volatile
    {
        BIT1 &= ~(1u << 16u);
    }

    /**
     * Toggle BIT1's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void toggle_BIT1_INV() volatile
    {
        BIT1 ^= 1u << 16u;
    }

    /**
     * Get BIT1's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline bool get_BIT1_CLK() volatile
    {
        return BIT1 & (1u << 17u);
    }

    /**
     * Set BIT1's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void set_BIT1_CLK() volatile
    {
        BIT1 |= 1u << 17u;
    }

    /**
     * Clear BIT1's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void clear_BIT1_CLK() volatile
    {
        BIT1 &= ~(1u << 17u);
    }

    /**
     * Toggle BIT1's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void toggle_BIT1_CLK() volatile
    {
        BIT1 ^= 1u << 17u;
    }

    /**
     * Get all of BIT1's bit fields.
     *
     * (read-write) Data control register for output bit 1
     */
    inline void get_BIT1(uint8_t &SEL_P, uint8_t &SEL_N, bool &INV,
                         bool &CLK) volatile
    {
        uint32_t curr = BIT1;

        SEL_P = curr & 0b11111u;
        SEL_N = (curr >> 8u) & 0b11111u;
        INV = curr & (1u << 16u);
        CLK = curr & (1u << 17u);
    }

    /**
     * Set all of BIT1's bit fields.
     *
     * (read-write) Data control register for output bit 1
     */
    inline void set_BIT1(uint8_t SEL_P, uint8_t SEL_N, bool INV,
                         bool CLK) volatile
    {
        uint32_t curr = BIT1;

        curr &= ~(0b11111u);
        curr |= (SEL_P & 0b11111u);
        curr &= ~(0b11111u << 8u);
        curr |= (SEL_N & 0b11111u) << 8u;
        curr &= ~(0b1u << 16u);
        curr |= (INV & 0b1u) << 16u;
        curr &= ~(0b1u << 17u);
        curr |= (CLK & 0b1u) << 17u;

        BIT1 = curr;
    }

    /**
     * Get BIT2's SEL_P field.
     *
     * Shift register data bit select for the first half of the HSTX clock
     * cycle
     */
    inline uint8_t get_BIT2_SEL_P() volatile
    {
        return BIT2 & 0b11111u;
    }

    /**
     * Set BIT2's SEL_P field.
     *
     * Shift register data bit select for the first half of the HSTX clock
     * cycle
     */
    inline void set_BIT2_SEL_P(uint8_t value) volatile
    {
        uint32_t curr = BIT2;

        curr &= ~(0b11111u);
        curr |= (value & 0b11111u);

        BIT2 = curr;
    }

    /**
     * Get BIT2's SEL_N field.
     *
     * Shift register data bit select for the second half of the HSTX clock
     * cycle
     */
    inline uint8_t get_BIT2_SEL_N() volatile
    {
        return (BIT2 >> 8u) & 0b11111u;
    }

    /**
     * Set BIT2's SEL_N field.
     *
     * Shift register data bit select for the second half of the HSTX clock
     * cycle
     */
    inline void set_BIT2_SEL_N(uint8_t value) volatile
    {
        uint32_t curr = BIT2;

        curr &= ~(0b11111u << 8u);
        curr |= (value & 0b11111u) << 8u;

        BIT2 = curr;
    }

    /**
     * Get BIT2's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline bool get_BIT2_INV() volatile
    {
        return BIT2 & (1u << 16u);
    }

    /**
     * Set BIT2's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void set_BIT2_INV() volatile
    {
        BIT2 |= 1u << 16u;
    }

    /**
     * Clear BIT2's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void clear_BIT2_INV() volatile
    {
        BIT2 &= ~(1u << 16u);
    }

    /**
     * Toggle BIT2's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void toggle_BIT2_INV() volatile
    {
        BIT2 ^= 1u << 16u;
    }

    /**
     * Get BIT2's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline bool get_BIT2_CLK() volatile
    {
        return BIT2 & (1u << 17u);
    }

    /**
     * Set BIT2's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void set_BIT2_CLK() volatile
    {
        BIT2 |= 1u << 17u;
    }

    /**
     * Clear BIT2's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void clear_BIT2_CLK() volatile
    {
        BIT2 &= ~(1u << 17u);
    }

    /**
     * Toggle BIT2's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void toggle_BIT2_CLK() volatile
    {
        BIT2 ^= 1u << 17u;
    }

    /**
     * Get all of BIT2's bit fields.
     *
     * (read-write) Data control register for output bit 2
     */
    inline void get_BIT2(uint8_t &SEL_P, uint8_t &SEL_N, bool &INV,
                         bool &CLK) volatile
    {
        uint32_t curr = BIT2;

        SEL_P = curr & 0b11111u;
        SEL_N = (curr >> 8u) & 0b11111u;
        INV = curr & (1u << 16u);
        CLK = curr & (1u << 17u);
    }

    /**
     * Set all of BIT2's bit fields.
     *
     * (read-write) Data control register for output bit 2
     */
    inline void set_BIT2(uint8_t SEL_P, uint8_t SEL_N, bool INV,
                         bool CLK) volatile
    {
        uint32_t curr = BIT2;

        curr &= ~(0b11111u);
        curr |= (SEL_P & 0b11111u);
        curr &= ~(0b11111u << 8u);
        curr |= (SEL_N & 0b11111u) << 8u;
        curr &= ~(0b1u << 16u);
        curr |= (INV & 0b1u) << 16u;
        curr &= ~(0b1u << 17u);
        curr |= (CLK & 0b1u) << 17u;

        BIT2 = curr;
    }

    /**
     * Get BIT3's SEL_P field.
     *
     * Shift register data bit select for the first half of the HSTX clock
     * cycle
     */
    inline uint8_t get_BIT3_SEL_P() volatile
    {
        return BIT3 & 0b11111u;
    }

    /**
     * Set BIT3's SEL_P field.
     *
     * Shift register data bit select for the first half of the HSTX clock
     * cycle
     */
    inline void set_BIT3_SEL_P(uint8_t value) volatile
    {
        uint32_t curr = BIT3;

        curr &= ~(0b11111u);
        curr |= (value & 0b11111u);

        BIT3 = curr;
    }

    /**
     * Get BIT3's SEL_N field.
     *
     * Shift register data bit select for the second half of the HSTX clock
     * cycle
     */
    inline uint8_t get_BIT3_SEL_N() volatile
    {
        return (BIT3 >> 8u) & 0b11111u;
    }

    /**
     * Set BIT3's SEL_N field.
     *
     * Shift register data bit select for the second half of the HSTX clock
     * cycle
     */
    inline void set_BIT3_SEL_N(uint8_t value) volatile
    {
        uint32_t curr = BIT3;

        curr &= ~(0b11111u << 8u);
        curr |= (value & 0b11111u) << 8u;

        BIT3 = curr;
    }

    /**
     * Get BIT3's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline bool get_BIT3_INV() volatile
    {
        return BIT3 & (1u << 16u);
    }

    /**
     * Set BIT3's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void set_BIT3_INV() volatile
    {
        BIT3 |= 1u << 16u;
    }

    /**
     * Clear BIT3's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void clear_BIT3_INV() volatile
    {
        BIT3 &= ~(1u << 16u);
    }

    /**
     * Toggle BIT3's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void toggle_BIT3_INV() volatile
    {
        BIT3 ^= 1u << 16u;
    }

    /**
     * Get BIT3's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline bool get_BIT3_CLK() volatile
    {
        return BIT3 & (1u << 17u);
    }

    /**
     * Set BIT3's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void set_BIT3_CLK() volatile
    {
        BIT3 |= 1u << 17u;
    }

    /**
     * Clear BIT3's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void clear_BIT3_CLK() volatile
    {
        BIT3 &= ~(1u << 17u);
    }

    /**
     * Toggle BIT3's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void toggle_BIT3_CLK() volatile
    {
        BIT3 ^= 1u << 17u;
    }

    /**
     * Get all of BIT3's bit fields.
     *
     * (read-write) Data control register for output bit 3
     */
    inline void get_BIT3(uint8_t &SEL_P, uint8_t &SEL_N, bool &INV,
                         bool &CLK) volatile
    {
        uint32_t curr = BIT3;

        SEL_P = curr & 0b11111u;
        SEL_N = (curr >> 8u) & 0b11111u;
        INV = curr & (1u << 16u);
        CLK = curr & (1u << 17u);
    }

    /**
     * Set all of BIT3's bit fields.
     *
     * (read-write) Data control register for output bit 3
     */
    inline void set_BIT3(uint8_t SEL_P, uint8_t SEL_N, bool INV,
                         bool CLK) volatile
    {
        uint32_t curr = BIT3;

        curr &= ~(0b11111u);
        curr |= (SEL_P & 0b11111u);
        curr &= ~(0b11111u << 8u);
        curr |= (SEL_N & 0b11111u) << 8u;
        curr &= ~(0b1u << 16u);
        curr |= (INV & 0b1u) << 16u;
        curr &= ~(0b1u << 17u);
        curr |= (CLK & 0b1u) << 17u;

        BIT3 = curr;
    }

    /**
     * Get BIT4's SEL_P field.
     *
     * Shift register data bit select for the first half of the HSTX clock
     * cycle
     */
    inline uint8_t get_BIT4_SEL_P() volatile
    {
        return BIT4 & 0b11111u;
    }

    /**
     * Set BIT4's SEL_P field.
     *
     * Shift register data bit select for the first half of the HSTX clock
     * cycle
     */
    inline void set_BIT4_SEL_P(uint8_t value) volatile
    {
        uint32_t curr = BIT4;

        curr &= ~(0b11111u);
        curr |= (value & 0b11111u);

        BIT4 = curr;
    }

    /**
     * Get BIT4's SEL_N field.
     *
     * Shift register data bit select for the second half of the HSTX clock
     * cycle
     */
    inline uint8_t get_BIT4_SEL_N() volatile
    {
        return (BIT4 >> 8u) & 0b11111u;
    }

    /**
     * Set BIT4's SEL_N field.
     *
     * Shift register data bit select for the second half of the HSTX clock
     * cycle
     */
    inline void set_BIT4_SEL_N(uint8_t value) volatile
    {
        uint32_t curr = BIT4;

        curr &= ~(0b11111u << 8u);
        curr |= (value & 0b11111u) << 8u;

        BIT4 = curr;
    }

    /**
     * Get BIT4's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline bool get_BIT4_INV() volatile
    {
        return BIT4 & (1u << 16u);
    }

    /**
     * Set BIT4's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void set_BIT4_INV() volatile
    {
        BIT4 |= 1u << 16u;
    }

    /**
     * Clear BIT4's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void clear_BIT4_INV() volatile
    {
        BIT4 &= ~(1u << 16u);
    }

    /**
     * Toggle BIT4's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void toggle_BIT4_INV() volatile
    {
        BIT4 ^= 1u << 16u;
    }

    /**
     * Get BIT4's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline bool get_BIT4_CLK() volatile
    {
        return BIT4 & (1u << 17u);
    }

    /**
     * Set BIT4's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void set_BIT4_CLK() volatile
    {
        BIT4 |= 1u << 17u;
    }

    /**
     * Clear BIT4's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void clear_BIT4_CLK() volatile
    {
        BIT4 &= ~(1u << 17u);
    }

    /**
     * Toggle BIT4's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void toggle_BIT4_CLK() volatile
    {
        BIT4 ^= 1u << 17u;
    }

    /**
     * Get all of BIT4's bit fields.
     *
     * (read-write) Data control register for output bit 4
     */
    inline void get_BIT4(uint8_t &SEL_P, uint8_t &SEL_N, bool &INV,
                         bool &CLK) volatile
    {
        uint32_t curr = BIT4;

        SEL_P = curr & 0b11111u;
        SEL_N = (curr >> 8u) & 0b11111u;
        INV = curr & (1u << 16u);
        CLK = curr & (1u << 17u);
    }

    /**
     * Set all of BIT4's bit fields.
     *
     * (read-write) Data control register for output bit 4
     */
    inline void set_BIT4(uint8_t SEL_P, uint8_t SEL_N, bool INV,
                         bool CLK) volatile
    {
        uint32_t curr = BIT4;

        curr &= ~(0b11111u);
        curr |= (SEL_P & 0b11111u);
        curr &= ~(0b11111u << 8u);
        curr |= (SEL_N & 0b11111u) << 8u;
        curr &= ~(0b1u << 16u);
        curr |= (INV & 0b1u) << 16u;
        curr &= ~(0b1u << 17u);
        curr |= (CLK & 0b1u) << 17u;

        BIT4 = curr;
    }

    /**
     * Get BIT5's SEL_P field.
     *
     * Shift register data bit select for the first half of the HSTX clock
     * cycle
     */
    inline uint8_t get_BIT5_SEL_P() volatile
    {
        return BIT5 & 0b11111u;
    }

    /**
     * Set BIT5's SEL_P field.
     *
     * Shift register data bit select for the first half of the HSTX clock
     * cycle
     */
    inline void set_BIT5_SEL_P(uint8_t value) volatile
    {
        uint32_t curr = BIT5;

        curr &= ~(0b11111u);
        curr |= (value & 0b11111u);

        BIT5 = curr;
    }

    /**
     * Get BIT5's SEL_N field.
     *
     * Shift register data bit select for the second half of the HSTX clock
     * cycle
     */
    inline uint8_t get_BIT5_SEL_N() volatile
    {
        return (BIT5 >> 8u) & 0b11111u;
    }

    /**
     * Set BIT5's SEL_N field.
     *
     * Shift register data bit select for the second half of the HSTX clock
     * cycle
     */
    inline void set_BIT5_SEL_N(uint8_t value) volatile
    {
        uint32_t curr = BIT5;

        curr &= ~(0b11111u << 8u);
        curr |= (value & 0b11111u) << 8u;

        BIT5 = curr;
    }

    /**
     * Get BIT5's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline bool get_BIT5_INV() volatile
    {
        return BIT5 & (1u << 16u);
    }

    /**
     * Set BIT5's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void set_BIT5_INV() volatile
    {
        BIT5 |= 1u << 16u;
    }

    /**
     * Clear BIT5's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void clear_BIT5_INV() volatile
    {
        BIT5 &= ~(1u << 16u);
    }

    /**
     * Toggle BIT5's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void toggle_BIT5_INV() volatile
    {
        BIT5 ^= 1u << 16u;
    }

    /**
     * Get BIT5's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline bool get_BIT5_CLK() volatile
    {
        return BIT5 & (1u << 17u);
    }

    /**
     * Set BIT5's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void set_BIT5_CLK() volatile
    {
        BIT5 |= 1u << 17u;
    }

    /**
     * Clear BIT5's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void clear_BIT5_CLK() volatile
    {
        BIT5 &= ~(1u << 17u);
    }

    /**
     * Toggle BIT5's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void toggle_BIT5_CLK() volatile
    {
        BIT5 ^= 1u << 17u;
    }

    /**
     * Get all of BIT5's bit fields.
     *
     * (read-write) Data control register for output bit 5
     */
    inline void get_BIT5(uint8_t &SEL_P, uint8_t &SEL_N, bool &INV,
                         bool &CLK) volatile
    {
        uint32_t curr = BIT5;

        SEL_P = curr & 0b11111u;
        SEL_N = (curr >> 8u) & 0b11111u;
        INV = curr & (1u << 16u);
        CLK = curr & (1u << 17u);
    }

    /**
     * Set all of BIT5's bit fields.
     *
     * (read-write) Data control register for output bit 5
     */
    inline void set_BIT5(uint8_t SEL_P, uint8_t SEL_N, bool INV,
                         bool CLK) volatile
    {
        uint32_t curr = BIT5;

        curr &= ~(0b11111u);
        curr |= (SEL_P & 0b11111u);
        curr &= ~(0b11111u << 8u);
        curr |= (SEL_N & 0b11111u) << 8u;
        curr &= ~(0b1u << 16u);
        curr |= (INV & 0b1u) << 16u;
        curr &= ~(0b1u << 17u);
        curr |= (CLK & 0b1u) << 17u;

        BIT5 = curr;
    }

    /**
     * Get BIT6's SEL_P field.
     *
     * Shift register data bit select for the first half of the HSTX clock
     * cycle
     */
    inline uint8_t get_BIT6_SEL_P() volatile
    {
        return BIT6 & 0b11111u;
    }

    /**
     * Set BIT6's SEL_P field.
     *
     * Shift register data bit select for the first half of the HSTX clock
     * cycle
     */
    inline void set_BIT6_SEL_P(uint8_t value) volatile
    {
        uint32_t curr = BIT6;

        curr &= ~(0b11111u);
        curr |= (value & 0b11111u);

        BIT6 = curr;
    }

    /**
     * Get BIT6's SEL_N field.
     *
     * Shift register data bit select for the second half of the HSTX clock
     * cycle
     */
    inline uint8_t get_BIT6_SEL_N() volatile
    {
        return (BIT6 >> 8u) & 0b11111u;
    }

    /**
     * Set BIT6's SEL_N field.
     *
     * Shift register data bit select for the second half of the HSTX clock
     * cycle
     */
    inline void set_BIT6_SEL_N(uint8_t value) volatile
    {
        uint32_t curr = BIT6;

        curr &= ~(0b11111u << 8u);
        curr |= (value & 0b11111u) << 8u;

        BIT6 = curr;
    }

    /**
     * Get BIT6's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline bool get_BIT6_INV() volatile
    {
        return BIT6 & (1u << 16u);
    }

    /**
     * Set BIT6's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void set_BIT6_INV() volatile
    {
        BIT6 |= 1u << 16u;
    }

    /**
     * Clear BIT6's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void clear_BIT6_INV() volatile
    {
        BIT6 &= ~(1u << 16u);
    }

    /**
     * Toggle BIT6's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void toggle_BIT6_INV() volatile
    {
        BIT6 ^= 1u << 16u;
    }

    /**
     * Get BIT6's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline bool get_BIT6_CLK() volatile
    {
        return BIT6 & (1u << 17u);
    }

    /**
     * Set BIT6's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void set_BIT6_CLK() volatile
    {
        BIT6 |= 1u << 17u;
    }

    /**
     * Clear BIT6's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void clear_BIT6_CLK() volatile
    {
        BIT6 &= ~(1u << 17u);
    }

    /**
     * Toggle BIT6's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void toggle_BIT6_CLK() volatile
    {
        BIT6 ^= 1u << 17u;
    }

    /**
     * Get all of BIT6's bit fields.
     *
     * (read-write) Data control register for output bit 6
     */
    inline void get_BIT6(uint8_t &SEL_P, uint8_t &SEL_N, bool &INV,
                         bool &CLK) volatile
    {
        uint32_t curr = BIT6;

        SEL_P = curr & 0b11111u;
        SEL_N = (curr >> 8u) & 0b11111u;
        INV = curr & (1u << 16u);
        CLK = curr & (1u << 17u);
    }

    /**
     * Set all of BIT6's bit fields.
     *
     * (read-write) Data control register for output bit 6
     */
    inline void set_BIT6(uint8_t SEL_P, uint8_t SEL_N, bool INV,
                         bool CLK) volatile
    {
        uint32_t curr = BIT6;

        curr &= ~(0b11111u);
        curr |= (SEL_P & 0b11111u);
        curr &= ~(0b11111u << 8u);
        curr |= (SEL_N & 0b11111u) << 8u;
        curr &= ~(0b1u << 16u);
        curr |= (INV & 0b1u) << 16u;
        curr &= ~(0b1u << 17u);
        curr |= (CLK & 0b1u) << 17u;

        BIT6 = curr;
    }

    /**
     * Get BIT7's SEL_P field.
     *
     * Shift register data bit select for the first half of the HSTX clock
     * cycle
     */
    inline uint8_t get_BIT7_SEL_P() volatile
    {
        return BIT7 & 0b11111u;
    }

    /**
     * Set BIT7's SEL_P field.
     *
     * Shift register data bit select for the first half of the HSTX clock
     * cycle
     */
    inline void set_BIT7_SEL_P(uint8_t value) volatile
    {
        uint32_t curr = BIT7;

        curr &= ~(0b11111u);
        curr |= (value & 0b11111u);

        BIT7 = curr;
    }

    /**
     * Get BIT7's SEL_N field.
     *
     * Shift register data bit select for the second half of the HSTX clock
     * cycle
     */
    inline uint8_t get_BIT7_SEL_N() volatile
    {
        return (BIT7 >> 8u) & 0b11111u;
    }

    /**
     * Set BIT7's SEL_N field.
     *
     * Shift register data bit select for the second half of the HSTX clock
     * cycle
     */
    inline void set_BIT7_SEL_N(uint8_t value) volatile
    {
        uint32_t curr = BIT7;

        curr &= ~(0b11111u << 8u);
        curr |= (value & 0b11111u) << 8u;

        BIT7 = curr;
    }

    /**
     * Get BIT7's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline bool get_BIT7_INV() volatile
    {
        return BIT7 & (1u << 16u);
    }

    /**
     * Set BIT7's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void set_BIT7_INV() volatile
    {
        BIT7 |= 1u << 16u;
    }

    /**
     * Clear BIT7's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void clear_BIT7_INV() volatile
    {
        BIT7 &= ~(1u << 16u);
    }

    /**
     * Toggle BIT7's INV bit.
     *
     * Invert this data output (logical NOT)
     */
    inline void toggle_BIT7_INV() volatile
    {
        BIT7 ^= 1u << 16u;
    }

    /**
     * Get BIT7's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline bool get_BIT7_CLK() volatile
    {
        return BIT7 & (1u << 17u);
    }

    /**
     * Set BIT7's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void set_BIT7_CLK() volatile
    {
        BIT7 |= 1u << 17u;
    }

    /**
     * Clear BIT7's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void clear_BIT7_CLK() volatile
    {
        BIT7 &= ~(1u << 17u);
    }

    /**
     * Toggle BIT7's CLK bit.
     *
     * Connect this output to the generated clock, rather than the data shift
     * register. SEL_P and SEL_N are ignored if this bit is set, but INV can
     * still be set to generate an antiphase clock.
     */
    inline void toggle_BIT7_CLK() volatile
    {
        BIT7 ^= 1u << 17u;
    }

    /**
     * Get all of BIT7's bit fields.
     *
     * (read-write) Data control register for output bit 7
     */
    inline void get_BIT7(uint8_t &SEL_P, uint8_t &SEL_N, bool &INV,
                         bool &CLK) volatile
    {
        uint32_t curr = BIT7;

        SEL_P = curr & 0b11111u;
        SEL_N = (curr >> 8u) & 0b11111u;
        INV = curr & (1u << 16u);
        CLK = curr & (1u << 17u);
    }

    /**
     * Set all of BIT7's bit fields.
     *
     * (read-write) Data control register for output bit 7
     */
    inline void set_BIT7(uint8_t SEL_P, uint8_t SEL_N, bool INV,
                         bool CLK) volatile
    {
        uint32_t curr = BIT7;

        curr &= ~(0b11111u);
        curr |= (SEL_P & 0b11111u);
        curr &= ~(0b11111u << 8u);
        curr |= (SEL_N & 0b11111u) << 8u;
        curr &= ~(0b1u << 16u);
        curr |= (INV & 0b1u) << 16u;
        curr &= ~(0b1u << 17u);
        curr |= (CLK & 0b1u) << 17u;

        BIT7 = curr;
    }

    /**
     * Get EXPAND_SHIFT's RAW_SHIFT field.
     *
     * How many bits to right-rotate the shift register by each time data is
     * pushed to the output shifter, when the current command is a raw data
     * command.
     */
    inline uint8_t get_EXPAND_SHIFT_RAW_SHIFT() volatile
    {
        return EXPAND_SHIFT & 0b11111u;
    }

    /**
     * Set EXPAND_SHIFT's RAW_SHIFT field.
     *
     * How many bits to right-rotate the shift register by each time data is
     * pushed to the output shifter, when the current command is a raw data
     * command.
     */
    inline void set_EXPAND_SHIFT_RAW_SHIFT(uint8_t value) volatile
    {
        uint32_t curr = EXPAND_SHIFT;

        curr &= ~(0b11111u);
        curr |= (value & 0b11111u);

        EXPAND_SHIFT = curr;
    }

    /**
     * Get EXPAND_SHIFT's RAW_N_SHIFTS field.
     *
     * Number of times to consume from the shift register before refilling it
     * from the FIFO, when the current command is a raw data command. A
     * register value of 0 means shift 32 times.
     */
    inline uint8_t get_EXPAND_SHIFT_RAW_N_SHIFTS() volatile
    {
        return (EXPAND_SHIFT >> 8u) & 0b11111u;
    }

    /**
     * Set EXPAND_SHIFT's RAW_N_SHIFTS field.
     *
     * Number of times to consume from the shift register before refilling it
     * from the FIFO, when the current command is a raw data command. A
     * register value of 0 means shift 32 times.
     */
    inline void set_EXPAND_SHIFT_RAW_N_SHIFTS(uint8_t value) volatile
    {
        uint32_t curr = EXPAND_SHIFT;

        curr &= ~(0b11111u << 8u);
        curr |= (value & 0b11111u) << 8u;

        EXPAND_SHIFT = curr;
    }

    /**
     * Get EXPAND_SHIFT's ENC_SHIFT field.
     *
     * How many bits to right-rotate the shift register by each time data is
     * pushed to the output shifter, when the current command is an encoded
     * data command (e.g. TMDS).
     */
    inline uint8_t get_EXPAND_SHIFT_ENC_SHIFT() volatile
    {
        return (EXPAND_SHIFT >> 16u) & 0b11111u;
    }

    /**
     * Set EXPAND_SHIFT's ENC_SHIFT field.
     *
     * How many bits to right-rotate the shift register by each time data is
     * pushed to the output shifter, when the current command is an encoded
     * data command (e.g. TMDS).
     */
    inline void set_EXPAND_SHIFT_ENC_SHIFT(uint8_t value) volatile
    {
        uint32_t curr = EXPAND_SHIFT;

        curr &= ~(0b11111u << 16u);
        curr |= (value & 0b11111u) << 16u;

        EXPAND_SHIFT = curr;
    }

    /**
     * Get EXPAND_SHIFT's ENC_N_SHIFTS field.
     *
     * Number of times to consume from the shift register before refilling it
     * from the FIFO, when the current command is an encoded data command (e.g.
     * TMDS). A register value of 0 means shift 32 times.
     */
    inline uint8_t get_EXPAND_SHIFT_ENC_N_SHIFTS() volatile
    {
        return (EXPAND_SHIFT >> 24u) & 0b11111u;
    }

    /**
     * Set EXPAND_SHIFT's ENC_N_SHIFTS field.
     *
     * Number of times to consume from the shift register before refilling it
     * from the FIFO, when the current command is an encoded data command (e.g.
     * TMDS). A register value of 0 means shift 32 times.
     */
    inline void set_EXPAND_SHIFT_ENC_N_SHIFTS(uint8_t value) volatile
    {
        uint32_t curr = EXPAND_SHIFT;

        curr &= ~(0b11111u << 24u);
        curr |= (value & 0b11111u) << 24u;

        EXPAND_SHIFT = curr;
    }

    /**
     * Get all of EXPAND_SHIFT's bit fields.
     *
     * (read-write) Configure the optional shifter inside the command expander
     */
    inline void get_EXPAND_SHIFT(uint8_t &RAW_SHIFT, uint8_t &RAW_N_SHIFTS,
                                 uint8_t &ENC_SHIFT,
                                 uint8_t &ENC_N_SHIFTS) volatile
    {
        uint32_t curr = EXPAND_SHIFT;

        RAW_SHIFT = curr & 0b11111u;
        RAW_N_SHIFTS = (curr >> 8u) & 0b11111u;
        ENC_SHIFT = (curr >> 16u) & 0b11111u;
        ENC_N_SHIFTS = (curr >> 24u) & 0b11111u;
    }

    /**
     * Set all of EXPAND_SHIFT's bit fields.
     *
     * (read-write) Configure the optional shifter inside the command expander
     */
    inline void set_EXPAND_SHIFT(uint8_t RAW_SHIFT, uint8_t RAW_N_SHIFTS,
                                 uint8_t ENC_SHIFT,
                                 uint8_t ENC_N_SHIFTS) volatile
    {
        uint32_t curr = EXPAND_SHIFT;

        curr &= ~(0b11111u);
        curr |= (RAW_SHIFT & 0b11111u);
        curr &= ~(0b11111u << 8u);
        curr |= (RAW_N_SHIFTS & 0b11111u) << 8u;
        curr &= ~(0b11111u << 16u);
        curr |= (ENC_SHIFT & 0b11111u) << 16u;
        curr &= ~(0b11111u << 24u);
        curr |= (ENC_N_SHIFTS & 0b11111u) << 24u;

        EXPAND_SHIFT = curr;
    }

    /**
     * Get EXPAND_TMDS's L0_ROT field.
     *
     * Right-rotate applied to the current shifter data before the lane 0 TMDS
     * encoder.
     */
    inline uint8_t get_EXPAND_TMDS_L0_ROT() volatile
    {
        return EXPAND_TMDS & 0b11111u;
    }

    /**
     * Set EXPAND_TMDS's L0_ROT field.
     *
     * Right-rotate applied to the current shifter data before the lane 0 TMDS
     * encoder.
     */
    inline void set_EXPAND_TMDS_L0_ROT(uint8_t value) volatile
    {
        uint32_t curr = EXPAND_TMDS;

        curr &= ~(0b11111u);
        curr |= (value & 0b11111u);

        EXPAND_TMDS = curr;
    }

    /**
     * Get EXPAND_TMDS's L0_NBITS field.
     *
     * Number of valid data bits for the lane 0 TMDS encoder, starting from bit
     * 7 of the rotated data. Field values of 0 -> 7 encode counts of 1 -> 8
     * bits.
     */
    inline uint8_t get_EXPAND_TMDS_L0_NBITS() volatile
    {
        return (EXPAND_TMDS >> 5u) & 0b111u;
    }

    /**
     * Set EXPAND_TMDS's L0_NBITS field.
     *
     * Number of valid data bits for the lane 0 TMDS encoder, starting from bit
     * 7 of the rotated data. Field values of 0 -> 7 encode counts of 1 -> 8
     * bits.
     */
    inline void set_EXPAND_TMDS_L0_NBITS(uint8_t value) volatile
    {
        uint32_t curr = EXPAND_TMDS;

        curr &= ~(0b111u << 5u);
        curr |= (value & 0b111u) << 5u;

        EXPAND_TMDS = curr;
    }

    /**
     * Get EXPAND_TMDS's L1_ROT field.
     *
     * Right-rotate applied to the current shifter data before the lane 1 TMDS
     * encoder.
     */
    inline uint8_t get_EXPAND_TMDS_L1_ROT() volatile
    {
        return (EXPAND_TMDS >> 8u) & 0b11111u;
    }

    /**
     * Set EXPAND_TMDS's L1_ROT field.
     *
     * Right-rotate applied to the current shifter data before the lane 1 TMDS
     * encoder.
     */
    inline void set_EXPAND_TMDS_L1_ROT(uint8_t value) volatile
    {
        uint32_t curr = EXPAND_TMDS;

        curr &= ~(0b11111u << 8u);
        curr |= (value & 0b11111u) << 8u;

        EXPAND_TMDS = curr;
    }

    /**
     * Get EXPAND_TMDS's L1_NBITS field.
     *
     * Number of valid data bits for the lane 1 TMDS encoder, starting from bit
     * 7 of the rotated data. Field values of 0 -> 7 encode counts of 1 -> 8
     * bits.
     */
    inline uint8_t get_EXPAND_TMDS_L1_NBITS() volatile
    {
        return (EXPAND_TMDS >> 13u) & 0b111u;
    }

    /**
     * Set EXPAND_TMDS's L1_NBITS field.
     *
     * Number of valid data bits for the lane 1 TMDS encoder, starting from bit
     * 7 of the rotated data. Field values of 0 -> 7 encode counts of 1 -> 8
     * bits.
     */
    inline void set_EXPAND_TMDS_L1_NBITS(uint8_t value) volatile
    {
        uint32_t curr = EXPAND_TMDS;

        curr &= ~(0b111u << 13u);
        curr |= (value & 0b111u) << 13u;

        EXPAND_TMDS = curr;
    }

    /**
     * Get EXPAND_TMDS's L2_ROT field.
     *
     * Right-rotate applied to the current shifter data before the lane 2 TMDS
     * encoder.
     */
    inline uint8_t get_EXPAND_TMDS_L2_ROT() volatile
    {
        return (EXPAND_TMDS >> 16u) & 0b11111u;
    }

    /**
     * Set EXPAND_TMDS's L2_ROT field.
     *
     * Right-rotate applied to the current shifter data before the lane 2 TMDS
     * encoder.
     */
    inline void set_EXPAND_TMDS_L2_ROT(uint8_t value) volatile
    {
        uint32_t curr = EXPAND_TMDS;

        curr &= ~(0b11111u << 16u);
        curr |= (value & 0b11111u) << 16u;

        EXPAND_TMDS = curr;
    }

    /**
     * Get EXPAND_TMDS's L2_NBITS field.
     *
     * Number of valid data bits for the lane 2 TMDS encoder, starting from bit
     * 7 of the rotated data. Field values of 0 -> 7 encode counts of 1 -> 8
     * bits.
     */
    inline uint8_t get_EXPAND_TMDS_L2_NBITS() volatile
    {
        return (EXPAND_TMDS >> 21u) & 0b111u;
    }

    /**
     * Set EXPAND_TMDS's L2_NBITS field.
     *
     * Number of valid data bits for the lane 2 TMDS encoder, starting from bit
     * 7 of the rotated data. Field values of 0 -> 7 encode counts of 1 -> 8
     * bits.
     */
    inline void set_EXPAND_TMDS_L2_NBITS(uint8_t value) volatile
    {
        uint32_t curr = EXPAND_TMDS;

        curr &= ~(0b111u << 21u);
        curr |= (value & 0b111u) << 21u;

        EXPAND_TMDS = curr;
    }

    /**
     * Get all of EXPAND_TMDS's bit fields.
     *
     * (read-write) Configure the optional TMDS encoder inside the command
     * expander
     */
    inline void get_EXPAND_TMDS(uint8_t &L0_ROT, uint8_t &L0_NBITS,
                                uint8_t &L1_ROT, uint8_t &L1_NBITS,
                                uint8_t &L2_ROT, uint8_t &L2_NBITS) volatile
    {
        uint32_t curr = EXPAND_TMDS;

        L0_ROT = curr & 0b11111u;
        L0_NBITS = (curr >> 5u) & 0b111u;
        L1_ROT = (curr >> 8u) & 0b11111u;
        L1_NBITS = (curr >> 13u) & 0b111u;
        L2_ROT = (curr >> 16u) & 0b11111u;
        L2_NBITS = (curr >> 21u) & 0b111u;
    }

    /**
     * Set all of EXPAND_TMDS's bit fields.
     *
     * (read-write) Configure the optional TMDS encoder inside the command
     * expander
     */
    inline void set_EXPAND_TMDS(uint8_t L0_ROT, uint8_t L0_NBITS,
                                uint8_t L1_ROT, uint8_t L1_NBITS,
                                uint8_t L2_ROT, uint8_t L2_NBITS) volatile
    {
        uint32_t curr = EXPAND_TMDS;

        curr &= ~(0b11111u);
        curr |= (L0_ROT & 0b11111u);
        curr &= ~(0b111u << 5u);
        curr |= (L0_NBITS & 0b111u) << 5u;
        curr &= ~(0b11111u << 8u);
        curr |= (L1_ROT & 0b11111u) << 8u;
        curr &= ~(0b111u << 13u);
        curr |= (L1_NBITS & 0b111u) << 13u;
        curr &= ~(0b11111u << 16u);
        curr |= (L2_ROT & 0b11111u) << 16u;
        curr &= ~(0b111u << 21u);
        curr |= (L2_NBITS & 0b111u) << 21u;

        EXPAND_TMDS = curr;
    }
};

static_assert(sizeof(hstx_ctrl) == hstx_ctrl::size);
static_assert(ifgen_struct<hstx_ctrl>);

static volatile hstx_ctrl *const HSTX_CTRL =
    reinterpret_cast<hstx_ctrl *>(0x400c0000);

}; // namespace RP2350

#endif
