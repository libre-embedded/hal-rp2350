/**
 * \file
 * \brief Generated by ifgen (4.7.7).
 */

#pragma once
#ifndef RP2350_STRUCTS_DMA_MPU_H
#define RP2350_STRUCTS_DMA_MPU_H

#include "../ifgen/common.h"

namespace RP2350
{

struct dma_mpu
{
    /* Constant attributes. */
    static constexpr struct_id_t id = 10;  /*!< dma_mpu's identifier. */
    static constexpr std::size_t size = 8; /*!< dma_mpu's size in bytes. */

    /* Fields. */
    uint32_t BAR; /*!< (read-write) Base address register for MPU region N.
                     Writable only from a Secure, Privileged context. */
    uint32_t LAR; /*!< (read-write) Limit address register for MPU region N.
                     Writable only from a Secure, Privileged context, with the
                     exception of the P bit. */

    /* Methods. */

    /**
     * Get BAR's ADDR field.
     *
     * This MPU region matches addresses where addr[31:5] (the 27 most
     * significant bits) are greater than or equal to BAR_ADDR, and less than
     * or equal to LAR_ADDR.
     *
     *                             Readable from any Privileged context, if and
     * only if this region's S bit is clear, and MPU_CTRL_NS_HIDE_ADDR is
     * clear. Otherwise readable only from a Secure, Privileged context.
     */
    inline uint32_t get_BAR() volatile
    {
        return (BAR >> 5u) & 0b111111111111111111111111111u;
    }

    /**
     * Set BAR's ADDR field.
     *
     * This MPU region matches addresses where addr[31:5] (the 27 most
     * significant bits) are greater than or equal to BAR_ADDR, and less than
     * or equal to LAR_ADDR.
     *
     *                             Readable from any Privileged context, if and
     * only if this region's S bit is clear, and MPU_CTRL_NS_HIDE_ADDR is
     * clear. Otherwise readable only from a Secure, Privileged context.
     */
    inline void set_BAR(uint32_t value) volatile
    {
        uint32_t curr = BAR;

        curr &= ~(0b111111111111111111111111111u << 5u);
        curr |= (value & 0b111111111111111111111111111u) << 5u;

        BAR = curr;
    }

    /**
     * Get LAR's EN bit.
     *
     * Region enable. If 1, any address within range specified by the base
     * address (BAR_ADDR) and limit address (LAR_ADDR) has the attributes
     * specified by S and P.
     */
    inline bool get_LAR_EN() volatile
    {
        return LAR & 1u;
    }

    /**
     * Set LAR's EN bit.
     *
     * Region enable. If 1, any address within range specified by the base
     * address (BAR_ADDR) and limit address (LAR_ADDR) has the attributes
     * specified by S and P.
     */
    inline void set_LAR_EN() volatile
    {
        LAR |= 1u;
    }

    /**
     * Clear LAR's EN bit.
     *
     * Region enable. If 1, any address within range specified by the base
     * address (BAR_ADDR) and limit address (LAR_ADDR) has the attributes
     * specified by S and P.
     */
    inline void clear_LAR_EN() volatile
    {
        LAR &= ~(1u);
    }

    /**
     * Toggle LAR's EN bit.
     *
     * Region enable. If 1, any address within range specified by the base
     * address (BAR_ADDR) and limit address (LAR_ADDR) has the attributes
     * specified by S and P.
     */
    inline void toggle_LAR_EN() volatile
    {
        LAR ^= 1u;
    }

    /**
     * Get LAR's P bit.
     *
     * Determines the Privileged/Unprivileged (=1/0) status of addresses
     * matching this region, if this region is enabled. Writable from any
     * Privileged context, if and only if the S bit is clear. Otherwise,
     * writable only from a Secure, Privileged context.
     */
    inline bool get_LAR_P() volatile
    {
        return LAR & (1u << 1u);
    }

    /**
     * Set LAR's P bit.
     *
     * Determines the Privileged/Unprivileged (=1/0) status of addresses
     * matching this region, if this region is enabled. Writable from any
     * Privileged context, if and only if the S bit is clear. Otherwise,
     * writable only from a Secure, Privileged context.
     */
    inline void set_LAR_P() volatile
    {
        LAR |= 1u << 1u;
    }

    /**
     * Clear LAR's P bit.
     *
     * Determines the Privileged/Unprivileged (=1/0) status of addresses
     * matching this region, if this region is enabled. Writable from any
     * Privileged context, if and only if the S bit is clear. Otherwise,
     * writable only from a Secure, Privileged context.
     */
    inline void clear_LAR_P() volatile
    {
        LAR &= ~(1u << 1u);
    }

    /**
     * Toggle LAR's P bit.
     *
     * Determines the Privileged/Unprivileged (=1/0) status of addresses
     * matching this region, if this region is enabled. Writable from any
     * Privileged context, if and only if the S bit is clear. Otherwise,
     * writable only from a Secure, Privileged context.
     */
    inline void toggle_LAR_P() volatile
    {
        LAR ^= 1u << 1u;
    }

    /**
     * Get LAR's S bit.
     *
     * Determines the Secure/Non-secure (=1/0) status of addresses matching
     * this region, if this region is enabled.
     */
    inline bool get_LAR_S() volatile
    {
        return LAR & (1u << 2u);
    }

    /**
     * Set LAR's S bit.
     *
     * Determines the Secure/Non-secure (=1/0) status of addresses matching
     * this region, if this region is enabled.
     */
    inline void set_LAR_S() volatile
    {
        LAR |= 1u << 2u;
    }

    /**
     * Clear LAR's S bit.
     *
     * Determines the Secure/Non-secure (=1/0) status of addresses matching
     * this region, if this region is enabled.
     */
    inline void clear_LAR_S() volatile
    {
        LAR &= ~(1u << 2u);
    }

    /**
     * Toggle LAR's S bit.
     *
     * Determines the Secure/Non-secure (=1/0) status of addresses matching
     * this region, if this region is enabled.
     */
    inline void toggle_LAR_S() volatile
    {
        LAR ^= 1u << 2u;
    }

    /**
     * Get LAR's ADDR field.
     *
     * Limit address bits 31:5. Readable from any Privileged context, if and
     * only if this region's S bit is clear, and MPU_CTRL_NS_HIDE_ADDR is
     * clear. Otherwise readable only from a Secure, Privileged context.
     */
    inline uint32_t get_LAR_ADDR() volatile
    {
        return (LAR >> 5u) & 0b111111111111111111111111111u;
    }

    /**
     * Set LAR's ADDR field.
     *
     * Limit address bits 31:5. Readable from any Privileged context, if and
     * only if this region's S bit is clear, and MPU_CTRL_NS_HIDE_ADDR is
     * clear. Otherwise readable only from a Secure, Privileged context.
     */
    inline void set_LAR_ADDR(uint32_t value) volatile
    {
        uint32_t curr = LAR;

        curr &= ~(0b111111111111111111111111111u << 5u);
        curr |= (value & 0b111111111111111111111111111u) << 5u;

        LAR = curr;
    }

    /**
     * Get all of LAR's bit fields.
     *
     * (read-write) Limit address register for MPU region N. Writable only from
     * a Secure, Privileged context, with the exception of the P bit.
     */
    inline void get_LAR(bool &EN, bool &P, bool &S, uint32_t &ADDR) volatile
    {
        uint32_t curr = LAR;

        EN = curr & 1u;
        P = curr & (1u << 1u);
        S = curr & (1u << 2u);
        ADDR = (curr >> 5u) & 0b111111111111111111111111111u;
    }

    /**
     * Set all of LAR's bit fields.
     *
     * (read-write) Limit address register for MPU region N. Writable only from
     * a Secure, Privileged context, with the exception of the P bit.
     */
    inline void set_LAR(bool EN, bool P, bool S, uint32_t ADDR) volatile
    {
        uint32_t curr = LAR;

        curr &= ~(0b1u);
        curr |= (EN & 0b1u);
        curr &= ~(0b1u << 1u);
        curr |= (P & 0b1u) << 1u;
        curr &= ~(0b1u << 2u);
        curr |= (S & 0b1u) << 2u;
        curr &= ~(0b111111111111111111111111111u << 5u);
        curr |= (ADDR & 0b111111111111111111111111111u) << 5u;

        LAR = curr;
    }
};

static_assert(sizeof(dma_mpu) == dma_mpu::size);
static_assert(ifgen_struct<dma_mpu>);

static volatile dma_mpu *const DMA_MPU_AR_NULL =
    reinterpret_cast<dma_mpu *>(0x00000000);

}; // namespace RP2350

#endif
