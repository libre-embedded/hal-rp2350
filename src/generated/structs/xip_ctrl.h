/**
 * \file
 * \brief Generated by ifgen (4.7.7).
 */

#pragma once
#ifndef RP2350_STRUCTS_XIP_CTRL_H
#define RP2350_STRUCTS_XIP_CTRL_H

#include "../ifgen/common.h"

namespace RP2350
{

/**
 * QSPI flash execute-in-place block
 */
struct xip_ctrl
{
    /* Constant attributes. */
    static constexpr struct_id_t id = 42;   /*!< xip_ctrl's identifier. */
    static constexpr std::size_t size = 32; /*!< xip_ctrl's size in bytes. */

    /* Fields. */
    uint32_t CTRL; /*!< (read-write) Cache control register. Read-only from a
                      Non-secure context. */
    const uint32_t reserved_padding0 = {};
    const uint32_t STAT = {};
    uint32_t CTR_HIT;                /*!< (read-write) Cache Hit counter */
    uint32_t CTR_ACC;                /*!< (read-write) Cache Access counter */
    uint32_t STREAM_ADDR;            /*!< (read-write) FIFO stream address */
    uint32_t STREAM_CTR;             /*!< (read-write) FIFO stream control */
    const uint32_t STREAM_FIFO = {}; /*!< (read-only) FIFO stream data */

    /* Methods. */

    /**
     * Get CTRL's EN_SECURE bit.
     *
     * When 1, enable the cache for Secure accesses. When enabled, Secure XIP
     * accesses to the cached (addr[26] == 0) window will query the cache, and
     * QSPI accesses are performed only if the requested data is not present.
     * When disabled, Secure access ignore the cache contents, and always
     * access the QSPI interface.
     *
     *                             Accesses to the uncached (addr[26] == 1)
     * window will never query the cache, irrespective of this bit.
     *
     *                             There is no cache-as-SRAM address window.
     * Cache lines are allocated for SRAM-like use by individually pinning
     * them, and keeping the cache enabled.
     */
    inline bool get_CTRL_EN_SECURE() volatile
    {
        return CTRL & 1u;
    }

    /**
     * Set CTRL's EN_SECURE bit.
     *
     * When 1, enable the cache for Secure accesses. When enabled, Secure XIP
     * accesses to the cached (addr[26] == 0) window will query the cache, and
     * QSPI accesses are performed only if the requested data is not present.
     * When disabled, Secure access ignore the cache contents, and always
     * access the QSPI interface.
     *
     *                             Accesses to the uncached (addr[26] == 1)
     * window will never query the cache, irrespective of this bit.
     *
     *                             There is no cache-as-SRAM address window.
     * Cache lines are allocated for SRAM-like use by individually pinning
     * them, and keeping the cache enabled.
     */
    inline void set_CTRL_EN_SECURE() volatile
    {
        CTRL |= 1u;
    }

    /**
     * Clear CTRL's EN_SECURE bit.
     *
     * When 1, enable the cache for Secure accesses. When enabled, Secure XIP
     * accesses to the cached (addr[26] == 0) window will query the cache, and
     * QSPI accesses are performed only if the requested data is not present.
     * When disabled, Secure access ignore the cache contents, and always
     * access the QSPI interface.
     *
     *                             Accesses to the uncached (addr[26] == 1)
     * window will never query the cache, irrespective of this bit.
     *
     *                             There is no cache-as-SRAM address window.
     * Cache lines are allocated for SRAM-like use by individually pinning
     * them, and keeping the cache enabled.
     */
    inline void clear_CTRL_EN_SECURE() volatile
    {
        CTRL &= ~(1u);
    }

    /**
     * Toggle CTRL's EN_SECURE bit.
     *
     * When 1, enable the cache for Secure accesses. When enabled, Secure XIP
     * accesses to the cached (addr[26] == 0) window will query the cache, and
     * QSPI accesses are performed only if the requested data is not present.
     * When disabled, Secure access ignore the cache contents, and always
     * access the QSPI interface.
     *
     *                             Accesses to the uncached (addr[26] == 1)
     * window will never query the cache, irrespective of this bit.
     *
     *                             There is no cache-as-SRAM address window.
     * Cache lines are allocated for SRAM-like use by individually pinning
     * them, and keeping the cache enabled.
     */
    inline void toggle_CTRL_EN_SECURE() volatile
    {
        CTRL ^= 1u;
    }

    /**
     * Get CTRL's EN_NONSECURE bit.
     *
     * When 1, enable the cache for Non-secure accesses. When enabled,
     * Non-secure XIP accesses to the cached (addr[26] == 0) window will query
     * the cache, and QSPI accesses are performed only if the requested data is
     * not present. When disabled, Secure access ignore the cache contents, and
     * always access the QSPI interface.
     *
     *                             Accesses to the uncached (addr[26] == 1)
     * window will never query the cache, irrespective of this bit.
     */
    inline bool get_CTRL_EN_NONSECURE() volatile
    {
        return CTRL & (1u << 1u);
    }

    /**
     * Set CTRL's EN_NONSECURE bit.
     *
     * When 1, enable the cache for Non-secure accesses. When enabled,
     * Non-secure XIP accesses to the cached (addr[26] == 0) window will query
     * the cache, and QSPI accesses are performed only if the requested data is
     * not present. When disabled, Secure access ignore the cache contents, and
     * always access the QSPI interface.
     *
     *                             Accesses to the uncached (addr[26] == 1)
     * window will never query the cache, irrespective of this bit.
     */
    inline void set_CTRL_EN_NONSECURE() volatile
    {
        CTRL |= 1u << 1u;
    }

    /**
     * Clear CTRL's EN_NONSECURE bit.
     *
     * When 1, enable the cache for Non-secure accesses. When enabled,
     * Non-secure XIP accesses to the cached (addr[26] == 0) window will query
     * the cache, and QSPI accesses are performed only if the requested data is
     * not present. When disabled, Secure access ignore the cache contents, and
     * always access the QSPI interface.
     *
     *                             Accesses to the uncached (addr[26] == 1)
     * window will never query the cache, irrespective of this bit.
     */
    inline void clear_CTRL_EN_NONSECURE() volatile
    {
        CTRL &= ~(1u << 1u);
    }

    /**
     * Toggle CTRL's EN_NONSECURE bit.
     *
     * When 1, enable the cache for Non-secure accesses. When enabled,
     * Non-secure XIP accesses to the cached (addr[26] == 0) window will query
     * the cache, and QSPI accesses are performed only if the requested data is
     * not present. When disabled, Secure access ignore the cache contents, and
     * always access the QSPI interface.
     *
     *                             Accesses to the uncached (addr[26] == 1)
     * window will never query the cache, irrespective of this bit.
     */
    inline void toggle_CTRL_EN_NONSECURE() volatile
    {
        CTRL ^= 1u << 1u;
    }

    /**
     * Get CTRL's POWER_DOWN bit.
     *
     * When 1, the cache memories are powered down. They retain state, but can
     * not be accessed. This reduces static power dissipation. Writing 1 to
     * this bit forces CTRL_EN_SECURE and CTRL_EN_NONSECURE to 0, i.e. the
     * cache cannot be enabled when powered down.
     */
    inline bool get_CTRL_POWER_DOWN() volatile
    {
        return CTRL & (1u << 3u);
    }

    /**
     * Set CTRL's POWER_DOWN bit.
     *
     * When 1, the cache memories are powered down. They retain state, but can
     * not be accessed. This reduces static power dissipation. Writing 1 to
     * this bit forces CTRL_EN_SECURE and CTRL_EN_NONSECURE to 0, i.e. the
     * cache cannot be enabled when powered down.
     */
    inline void set_CTRL_POWER_DOWN() volatile
    {
        CTRL |= 1u << 3u;
    }

    /**
     * Clear CTRL's POWER_DOWN bit.
     *
     * When 1, the cache memories are powered down. They retain state, but can
     * not be accessed. This reduces static power dissipation. Writing 1 to
     * this bit forces CTRL_EN_SECURE and CTRL_EN_NONSECURE to 0, i.e. the
     * cache cannot be enabled when powered down.
     */
    inline void clear_CTRL_POWER_DOWN() volatile
    {
        CTRL &= ~(1u << 3u);
    }

    /**
     * Toggle CTRL's POWER_DOWN bit.
     *
     * When 1, the cache memories are powered down. They retain state, but can
     * not be accessed. This reduces static power dissipation. Writing 1 to
     * this bit forces CTRL_EN_SECURE and CTRL_EN_NONSECURE to 0, i.e. the
     * cache cannot be enabled when powered down.
     */
    inline void toggle_CTRL_POWER_DOWN() volatile
    {
        CTRL ^= 1u << 3u;
    }

    /**
     * Get CTRL's NO_UNCACHED_SEC bit.
     *
     * When 1, Secure accesses to the uncached window (addr[27:26] == 1) will
     * generate a bus error. This may reduce the number of SAU/MPU/PMP regions
     * required to protect flash contents.
     *
     *                             Note this does not disable access to the
     * uncached, untranslated window -- see NO_UNTRANSLATED_SEC.
     */
    inline bool get_CTRL_NO_UNCACHED_SEC() volatile
    {
        return CTRL & (1u << 4u);
    }

    /**
     * Set CTRL's NO_UNCACHED_SEC bit.
     *
     * When 1, Secure accesses to the uncached window (addr[27:26] == 1) will
     * generate a bus error. This may reduce the number of SAU/MPU/PMP regions
     * required to protect flash contents.
     *
     *                             Note this does not disable access to the
     * uncached, untranslated window -- see NO_UNTRANSLATED_SEC.
     */
    inline void set_CTRL_NO_UNCACHED_SEC() volatile
    {
        CTRL |= 1u << 4u;
    }

    /**
     * Clear CTRL's NO_UNCACHED_SEC bit.
     *
     * When 1, Secure accesses to the uncached window (addr[27:26] == 1) will
     * generate a bus error. This may reduce the number of SAU/MPU/PMP regions
     * required to protect flash contents.
     *
     *                             Note this does not disable access to the
     * uncached, untranslated window -- see NO_UNTRANSLATED_SEC.
     */
    inline void clear_CTRL_NO_UNCACHED_SEC() volatile
    {
        CTRL &= ~(1u << 4u);
    }

    /**
     * Toggle CTRL's NO_UNCACHED_SEC bit.
     *
     * When 1, Secure accesses to the uncached window (addr[27:26] == 1) will
     * generate a bus error. This may reduce the number of SAU/MPU/PMP regions
     * required to protect flash contents.
     *
     *                             Note this does not disable access to the
     * uncached, untranslated window -- see NO_UNTRANSLATED_SEC.
     */
    inline void toggle_CTRL_NO_UNCACHED_SEC() volatile
    {
        CTRL ^= 1u << 4u;
    }

    /**
     * Get CTRL's NO_UNCACHED_NONSEC bit.
     *
     * When 1, Non-secure accesses to the uncached window (addr[27:26] == 1)
     * will generate a bus error. This may reduce the number of SAU/MPU/PMP
     * regions required to protect flash contents.
     *
     *                             Note this does not disable access to the
     * uncached, untranslated window -- see NO_UNTRANSLATED_SEC.
     */
    inline bool get_CTRL_NO_UNCACHED_NONSEC() volatile
    {
        return CTRL & (1u << 5u);
    }

    /**
     * Set CTRL's NO_UNCACHED_NONSEC bit.
     *
     * When 1, Non-secure accesses to the uncached window (addr[27:26] == 1)
     * will generate a bus error. This may reduce the number of SAU/MPU/PMP
     * regions required to protect flash contents.
     *
     *                             Note this does not disable access to the
     * uncached, untranslated window -- see NO_UNTRANSLATED_SEC.
     */
    inline void set_CTRL_NO_UNCACHED_NONSEC() volatile
    {
        CTRL |= 1u << 5u;
    }

    /**
     * Clear CTRL's NO_UNCACHED_NONSEC bit.
     *
     * When 1, Non-secure accesses to the uncached window (addr[27:26] == 1)
     * will generate a bus error. This may reduce the number of SAU/MPU/PMP
     * regions required to protect flash contents.
     *
     *                             Note this does not disable access to the
     * uncached, untranslated window -- see NO_UNTRANSLATED_SEC.
     */
    inline void clear_CTRL_NO_UNCACHED_NONSEC() volatile
    {
        CTRL &= ~(1u << 5u);
    }

    /**
     * Toggle CTRL's NO_UNCACHED_NONSEC bit.
     *
     * When 1, Non-secure accesses to the uncached window (addr[27:26] == 1)
     * will generate a bus error. This may reduce the number of SAU/MPU/PMP
     * regions required to protect flash contents.
     *
     *                             Note this does not disable access to the
     * uncached, untranslated window -- see NO_UNTRANSLATED_SEC.
     */
    inline void toggle_CTRL_NO_UNCACHED_NONSEC() volatile
    {
        CTRL ^= 1u << 5u;
    }

    /**
     * Get CTRL's NO_UNTRANSLATED_SEC bit.
     *
     * When 1, Secure accesses to the uncached, untranslated window
     * (addr[27:26] == 3) will generate a bus error.
     */
    inline bool get_CTRL_NO_UNTRANSLATED_SEC() volatile
    {
        return CTRL & (1u << 6u);
    }

    /**
     * Set CTRL's NO_UNTRANSLATED_SEC bit.
     *
     * When 1, Secure accesses to the uncached, untranslated window
     * (addr[27:26] == 3) will generate a bus error.
     */
    inline void set_CTRL_NO_UNTRANSLATED_SEC() volatile
    {
        CTRL |= 1u << 6u;
    }

    /**
     * Clear CTRL's NO_UNTRANSLATED_SEC bit.
     *
     * When 1, Secure accesses to the uncached, untranslated window
     * (addr[27:26] == 3) will generate a bus error.
     */
    inline void clear_CTRL_NO_UNTRANSLATED_SEC() volatile
    {
        CTRL &= ~(1u << 6u);
    }

    /**
     * Toggle CTRL's NO_UNTRANSLATED_SEC bit.
     *
     * When 1, Secure accesses to the uncached, untranslated window
     * (addr[27:26] == 3) will generate a bus error.
     */
    inline void toggle_CTRL_NO_UNTRANSLATED_SEC() volatile
    {
        CTRL ^= 1u << 6u;
    }

    /**
     * Get CTRL's NO_UNTRANSLATED_NONSEC bit.
     *
     * When 1, Non-secure accesses to the uncached, untranslated window
     * (addr[27:26] == 3) will generate a bus error.
     */
    inline bool get_CTRL_NO_UNTRANSLATED_NONSEC() volatile
    {
        return CTRL & (1u << 7u);
    }

    /**
     * Set CTRL's NO_UNTRANSLATED_NONSEC bit.
     *
     * When 1, Non-secure accesses to the uncached, untranslated window
     * (addr[27:26] == 3) will generate a bus error.
     */
    inline void set_CTRL_NO_UNTRANSLATED_NONSEC() volatile
    {
        CTRL |= 1u << 7u;
    }

    /**
     * Clear CTRL's NO_UNTRANSLATED_NONSEC bit.
     *
     * When 1, Non-secure accesses to the uncached, untranslated window
     * (addr[27:26] == 3) will generate a bus error.
     */
    inline void clear_CTRL_NO_UNTRANSLATED_NONSEC() volatile
    {
        CTRL &= ~(1u << 7u);
    }

    /**
     * Toggle CTRL's NO_UNTRANSLATED_NONSEC bit.
     *
     * When 1, Non-secure accesses to the uncached, untranslated window
     * (addr[27:26] == 3) will generate a bus error.
     */
    inline void toggle_CTRL_NO_UNTRANSLATED_NONSEC() volatile
    {
        CTRL ^= 1u << 7u;
    }

    /**
     * Get CTRL's MAINT_NONSEC bit.
     *
     * When 0, Non-secure accesses to the cache maintenance address window
     * (addr[27] == 1, addr[26] == 0) will generate a bus error. When 1,
     * Non-secure accesses can perform cache maintenance operations by writing
     * to the cache maintenance address window.
     *
     *                             Cache maintenance operations may be used to
     * corrupt Secure data by invalidating cache lines inappropriately, or map
     * Secure content into a Non-secure region by pinning cache lines.
     * Therefore this bit should generally be set to 0, unless Secure code is
     * not using the cache.
     *
     *                             Care should also be taken to clear the cache
     * data memory and tag memory before granting maintenance operations to
     * Non-secure code.
     */
    inline bool get_CTRL_MAINT_NONSEC() volatile
    {
        return CTRL & (1u << 8u);
    }

    /**
     * Set CTRL's MAINT_NONSEC bit.
     *
     * When 0, Non-secure accesses to the cache maintenance address window
     * (addr[27] == 1, addr[26] == 0) will generate a bus error. When 1,
     * Non-secure accesses can perform cache maintenance operations by writing
     * to the cache maintenance address window.
     *
     *                             Cache maintenance operations may be used to
     * corrupt Secure data by invalidating cache lines inappropriately, or map
     * Secure content into a Non-secure region by pinning cache lines.
     * Therefore this bit should generally be set to 0, unless Secure code is
     * not using the cache.
     *
     *                             Care should also be taken to clear the cache
     * data memory and tag memory before granting maintenance operations to
     * Non-secure code.
     */
    inline void set_CTRL_MAINT_NONSEC() volatile
    {
        CTRL |= 1u << 8u;
    }

    /**
     * Clear CTRL's MAINT_NONSEC bit.
     *
     * When 0, Non-secure accesses to the cache maintenance address window
     * (addr[27] == 1, addr[26] == 0) will generate a bus error. When 1,
     * Non-secure accesses can perform cache maintenance operations by writing
     * to the cache maintenance address window.
     *
     *                             Cache maintenance operations may be used to
     * corrupt Secure data by invalidating cache lines inappropriately, or map
     * Secure content into a Non-secure region by pinning cache lines.
     * Therefore this bit should generally be set to 0, unless Secure code is
     * not using the cache.
     *
     *                             Care should also be taken to clear the cache
     * data memory and tag memory before granting maintenance operations to
     * Non-secure code.
     */
    inline void clear_CTRL_MAINT_NONSEC() volatile
    {
        CTRL &= ~(1u << 8u);
    }

    /**
     * Toggle CTRL's MAINT_NONSEC bit.
     *
     * When 0, Non-secure accesses to the cache maintenance address window
     * (addr[27] == 1, addr[26] == 0) will generate a bus error. When 1,
     * Non-secure accesses can perform cache maintenance operations by writing
     * to the cache maintenance address window.
     *
     *                             Cache maintenance operations may be used to
     * corrupt Secure data by invalidating cache lines inappropriately, or map
     * Secure content into a Non-secure region by pinning cache lines.
     * Therefore this bit should generally be set to 0, unless Secure code is
     * not using the cache.
     *
     *                             Care should also be taken to clear the cache
     * data memory and tag memory before granting maintenance operations to
     * Non-secure code.
     */
    inline void toggle_CTRL_MAINT_NONSEC() volatile
    {
        CTRL ^= 1u << 8u;
    }

    /**
     * Get CTRL's SPLIT_WAYS bit.
     *
     * When 1, route all cached+Secure accesses to way 0 of the cache, and
     * route all cached+Non-secure accesses to way 1 of the cache.
     *
     *                             This partitions the cache into two
     * half-sized direct-mapped regions, such that Non-secure code can not
     * observe cache line state changes caused by Secure execution.
     *
     *                             A full cache flush is required when changing
     * the value of SPLIT_WAYS. The flush should be performed whilst SPLIT_WAYS
     * is 0, so that both cache ways are accessible for invalidation.
     */
    inline bool get_CTRL_SPLIT_WAYS() volatile
    {
        return CTRL & (1u << 9u);
    }

    /**
     * Set CTRL's SPLIT_WAYS bit.
     *
     * When 1, route all cached+Secure accesses to way 0 of the cache, and
     * route all cached+Non-secure accesses to way 1 of the cache.
     *
     *                             This partitions the cache into two
     * half-sized direct-mapped regions, such that Non-secure code can not
     * observe cache line state changes caused by Secure execution.
     *
     *                             A full cache flush is required when changing
     * the value of SPLIT_WAYS. The flush should be performed whilst SPLIT_WAYS
     * is 0, so that both cache ways are accessible for invalidation.
     */
    inline void set_CTRL_SPLIT_WAYS() volatile
    {
        CTRL |= 1u << 9u;
    }

    /**
     * Clear CTRL's SPLIT_WAYS bit.
     *
     * When 1, route all cached+Secure accesses to way 0 of the cache, and
     * route all cached+Non-secure accesses to way 1 of the cache.
     *
     *                             This partitions the cache into two
     * half-sized direct-mapped regions, such that Non-secure code can not
     * observe cache line state changes caused by Secure execution.
     *
     *                             A full cache flush is required when changing
     * the value of SPLIT_WAYS. The flush should be performed whilst SPLIT_WAYS
     * is 0, so that both cache ways are accessible for invalidation.
     */
    inline void clear_CTRL_SPLIT_WAYS() volatile
    {
        CTRL &= ~(1u << 9u);
    }

    /**
     * Toggle CTRL's SPLIT_WAYS bit.
     *
     * When 1, route all cached+Secure accesses to way 0 of the cache, and
     * route all cached+Non-secure accesses to way 1 of the cache.
     *
     *                             This partitions the cache into two
     * half-sized direct-mapped regions, such that Non-secure code can not
     * observe cache line state changes caused by Secure execution.
     *
     *                             A full cache flush is required when changing
     * the value of SPLIT_WAYS. The flush should be performed whilst SPLIT_WAYS
     * is 0, so that both cache ways are accessible for invalidation.
     */
    inline void toggle_CTRL_SPLIT_WAYS() volatile
    {
        CTRL ^= 1u << 9u;
    }

    /**
     * Get CTRL's WRITABLE_M0 bit.
     *
     * If 1, enable writes to XIP memory window 0 (addresses 0x10000000 through
     * 0x10ffffff, and their uncached mirrors). If 0, this region is read-only.
     *
     *                             XIP memory is *read-only by default*. This
     * bit must be set to enable writes if a RAM device is attached on QSPI
     * chip select 0.
     *
     *                             The default read-only behaviour avoids two
     * issues with writing to a read-only QSPI device (e.g. flash). First, a
     * write will initially appear to succeed due to caching, but the data will
     * eventually be lost when the written line is evicted, causing
     * unpredictable behaviour.
     *
     *                             Second, when a written line is evicted, it
     * will cause a write command to be issued to the flash, which can break
     * the flash out of its continuous read mode. After this point, flash reads
     * will return garbage. This is a security concern, as it allows Non-secure
     * software to break Secure flash reads if it has permission to write to
     * any flash address.
     *
     *                             Note the read-only behaviour is implemented
     * by downgrading writes to reads, so writes will still cause allocation of
     * an address, but have no other effect.
     */
    inline bool get_CTRL_WRITABLE_M0() volatile
    {
        return CTRL & (1u << 10u);
    }

    /**
     * Set CTRL's WRITABLE_M0 bit.
     *
     * If 1, enable writes to XIP memory window 0 (addresses 0x10000000 through
     * 0x10ffffff, and their uncached mirrors). If 0, this region is read-only.
     *
     *                             XIP memory is *read-only by default*. This
     * bit must be set to enable writes if a RAM device is attached on QSPI
     * chip select 0.
     *
     *                             The default read-only behaviour avoids two
     * issues with writing to a read-only QSPI device (e.g. flash). First, a
     * write will initially appear to succeed due to caching, but the data will
     * eventually be lost when the written line is evicted, causing
     * unpredictable behaviour.
     *
     *                             Second, when a written line is evicted, it
     * will cause a write command to be issued to the flash, which can break
     * the flash out of its continuous read mode. After this point, flash reads
     * will return garbage. This is a security concern, as it allows Non-secure
     * software to break Secure flash reads if it has permission to write to
     * any flash address.
     *
     *                             Note the read-only behaviour is implemented
     * by downgrading writes to reads, so writes will still cause allocation of
     * an address, but have no other effect.
     */
    inline void set_CTRL_WRITABLE_M0() volatile
    {
        CTRL |= 1u << 10u;
    }

    /**
     * Clear CTRL's WRITABLE_M0 bit.
     *
     * If 1, enable writes to XIP memory window 0 (addresses 0x10000000 through
     * 0x10ffffff, and their uncached mirrors). If 0, this region is read-only.
     *
     *                             XIP memory is *read-only by default*. This
     * bit must be set to enable writes if a RAM device is attached on QSPI
     * chip select 0.
     *
     *                             The default read-only behaviour avoids two
     * issues with writing to a read-only QSPI device (e.g. flash). First, a
     * write will initially appear to succeed due to caching, but the data will
     * eventually be lost when the written line is evicted, causing
     * unpredictable behaviour.
     *
     *                             Second, when a written line is evicted, it
     * will cause a write command to be issued to the flash, which can break
     * the flash out of its continuous read mode. After this point, flash reads
     * will return garbage. This is a security concern, as it allows Non-secure
     * software to break Secure flash reads if it has permission to write to
     * any flash address.
     *
     *                             Note the read-only behaviour is implemented
     * by downgrading writes to reads, so writes will still cause allocation of
     * an address, but have no other effect.
     */
    inline void clear_CTRL_WRITABLE_M0() volatile
    {
        CTRL &= ~(1u << 10u);
    }

    /**
     * Toggle CTRL's WRITABLE_M0 bit.
     *
     * If 1, enable writes to XIP memory window 0 (addresses 0x10000000 through
     * 0x10ffffff, and their uncached mirrors). If 0, this region is read-only.
     *
     *                             XIP memory is *read-only by default*. This
     * bit must be set to enable writes if a RAM device is attached on QSPI
     * chip select 0.
     *
     *                             The default read-only behaviour avoids two
     * issues with writing to a read-only QSPI device (e.g. flash). First, a
     * write will initially appear to succeed due to caching, but the data will
     * eventually be lost when the written line is evicted, causing
     * unpredictable behaviour.
     *
     *                             Second, when a written line is evicted, it
     * will cause a write command to be issued to the flash, which can break
     * the flash out of its continuous read mode. After this point, flash reads
     * will return garbage. This is a security concern, as it allows Non-secure
     * software to break Secure flash reads if it has permission to write to
     * any flash address.
     *
     *                             Note the read-only behaviour is implemented
     * by downgrading writes to reads, so writes will still cause allocation of
     * an address, but have no other effect.
     */
    inline void toggle_CTRL_WRITABLE_M0() volatile
    {
        CTRL ^= 1u << 10u;
    }

    /**
     * Get CTRL's WRITABLE_M1 bit.
     *
     * If 1, enable writes to XIP memory window 1 (addresses 0x11000000 through
     * 0x11ffffff, and their uncached mirrors). If 0, this region is read-only.
     *
     *                             XIP memory is *read-only by default*. This
     * bit must be set to enable writes if a RAM device is attached on QSPI
     * chip select 1.
     *
     *                             The default read-only behaviour avoids two
     * issues with writing to a read-only QSPI device (e.g. flash). First, a
     * write will initially appear to succeed due to caching, but the data will
     * eventually be lost when the written line is evicted, causing
     * unpredictable behaviour.
     *
     *                             Second, when a written line is evicted, it
     * will cause a write command to be issued to the flash, which can break
     * the flash out of its continuous read mode. After this point, flash reads
     * will return garbage. This is a security concern, as it allows Non-secure
     * software to break Secure flash reads if it has permission to write to
     * any flash address.
     *
     *                             Note the read-only behaviour is implemented
     * by downgrading writes to reads, so writes will still cause allocation of
     * an address, but have no other effect.
     */
    inline bool get_CTRL_WRITABLE_M1() volatile
    {
        return CTRL & (1u << 11u);
    }

    /**
     * Set CTRL's WRITABLE_M1 bit.
     *
     * If 1, enable writes to XIP memory window 1 (addresses 0x11000000 through
     * 0x11ffffff, and their uncached mirrors). If 0, this region is read-only.
     *
     *                             XIP memory is *read-only by default*. This
     * bit must be set to enable writes if a RAM device is attached on QSPI
     * chip select 1.
     *
     *                             The default read-only behaviour avoids two
     * issues with writing to a read-only QSPI device (e.g. flash). First, a
     * write will initially appear to succeed due to caching, but the data will
     * eventually be lost when the written line is evicted, causing
     * unpredictable behaviour.
     *
     *                             Second, when a written line is evicted, it
     * will cause a write command to be issued to the flash, which can break
     * the flash out of its continuous read mode. After this point, flash reads
     * will return garbage. This is a security concern, as it allows Non-secure
     * software to break Secure flash reads if it has permission to write to
     * any flash address.
     *
     *                             Note the read-only behaviour is implemented
     * by downgrading writes to reads, so writes will still cause allocation of
     * an address, but have no other effect.
     */
    inline void set_CTRL_WRITABLE_M1() volatile
    {
        CTRL |= 1u << 11u;
    }

    /**
     * Clear CTRL's WRITABLE_M1 bit.
     *
     * If 1, enable writes to XIP memory window 1 (addresses 0x11000000 through
     * 0x11ffffff, and their uncached mirrors). If 0, this region is read-only.
     *
     *                             XIP memory is *read-only by default*. This
     * bit must be set to enable writes if a RAM device is attached on QSPI
     * chip select 1.
     *
     *                             The default read-only behaviour avoids two
     * issues with writing to a read-only QSPI device (e.g. flash). First, a
     * write will initially appear to succeed due to caching, but the data will
     * eventually be lost when the written line is evicted, causing
     * unpredictable behaviour.
     *
     *                             Second, when a written line is evicted, it
     * will cause a write command to be issued to the flash, which can break
     * the flash out of its continuous read mode. After this point, flash reads
     * will return garbage. This is a security concern, as it allows Non-secure
     * software to break Secure flash reads if it has permission to write to
     * any flash address.
     *
     *                             Note the read-only behaviour is implemented
     * by downgrading writes to reads, so writes will still cause allocation of
     * an address, but have no other effect.
     */
    inline void clear_CTRL_WRITABLE_M1() volatile
    {
        CTRL &= ~(1u << 11u);
    }

    /**
     * Toggle CTRL's WRITABLE_M1 bit.
     *
     * If 1, enable writes to XIP memory window 1 (addresses 0x11000000 through
     * 0x11ffffff, and their uncached mirrors). If 0, this region is read-only.
     *
     *                             XIP memory is *read-only by default*. This
     * bit must be set to enable writes if a RAM device is attached on QSPI
     * chip select 1.
     *
     *                             The default read-only behaviour avoids two
     * issues with writing to a read-only QSPI device (e.g. flash). First, a
     * write will initially appear to succeed due to caching, but the data will
     * eventually be lost when the written line is evicted, causing
     * unpredictable behaviour.
     *
     *                             Second, when a written line is evicted, it
     * will cause a write command to be issued to the flash, which can break
     * the flash out of its continuous read mode. After this point, flash reads
     * will return garbage. This is a security concern, as it allows Non-secure
     * software to break Secure flash reads if it has permission to write to
     * any flash address.
     *
     *                             Note the read-only behaviour is implemented
     * by downgrading writes to reads, so writes will still cause allocation of
     * an address, but have no other effect.
     */
    inline void toggle_CTRL_WRITABLE_M1() volatile
    {
        CTRL ^= 1u << 11u;
    }

    /**
     * Get all of CTRL's bit fields.
     *
     * (read-write) Cache control register. Read-only from a Non-secure
     * context.
     */
    inline void get_CTRL(bool &EN_SECURE, bool &EN_NONSECURE, bool &POWER_DOWN,
                         bool &NO_UNCACHED_SEC, bool &NO_UNCACHED_NONSEC,
                         bool &NO_UNTRANSLATED_SEC,
                         bool &NO_UNTRANSLATED_NONSEC, bool &MAINT_NONSEC,
                         bool &SPLIT_WAYS, bool &WRITABLE_M0,
                         bool &WRITABLE_M1) volatile
    {
        uint32_t curr = CTRL;

        EN_SECURE = curr & 1u;
        EN_NONSECURE = curr & (1u << 1u);
        POWER_DOWN = curr & (1u << 3u);
        NO_UNCACHED_SEC = curr & (1u << 4u);
        NO_UNCACHED_NONSEC = curr & (1u << 5u);
        NO_UNTRANSLATED_SEC = curr & (1u << 6u);
        NO_UNTRANSLATED_NONSEC = curr & (1u << 7u);
        MAINT_NONSEC = curr & (1u << 8u);
        SPLIT_WAYS = curr & (1u << 9u);
        WRITABLE_M0 = curr & (1u << 10u);
        WRITABLE_M1 = curr & (1u << 11u);
    }

    /**
     * Set all of CTRL's bit fields.
     *
     * (read-write) Cache control register. Read-only from a Non-secure
     * context.
     */
    inline void set_CTRL(bool EN_SECURE, bool EN_NONSECURE, bool POWER_DOWN,
                         bool NO_UNCACHED_SEC, bool NO_UNCACHED_NONSEC,
                         bool NO_UNTRANSLATED_SEC, bool NO_UNTRANSLATED_NONSEC,
                         bool MAINT_NONSEC, bool SPLIT_WAYS, bool WRITABLE_M0,
                         bool WRITABLE_M1) volatile
    {
        uint32_t curr = CTRL;

        curr &= ~(0b1u);
        curr |= (EN_SECURE & 0b1u);
        curr &= ~(0b1u << 1u);
        curr |= (EN_NONSECURE & 0b1u) << 1u;
        curr &= ~(0b1u << 3u);
        curr |= (POWER_DOWN & 0b1u) << 3u;
        curr &= ~(0b1u << 4u);
        curr |= (NO_UNCACHED_SEC & 0b1u) << 4u;
        curr &= ~(0b1u << 5u);
        curr |= (NO_UNCACHED_NONSEC & 0b1u) << 5u;
        curr &= ~(0b1u << 6u);
        curr |= (NO_UNTRANSLATED_SEC & 0b1u) << 6u;
        curr &= ~(0b1u << 7u);
        curr |= (NO_UNTRANSLATED_NONSEC & 0b1u) << 7u;
        curr &= ~(0b1u << 8u);
        curr |= (MAINT_NONSEC & 0b1u) << 8u;
        curr &= ~(0b1u << 9u);
        curr |= (SPLIT_WAYS & 0b1u) << 9u;
        curr &= ~(0b1u << 10u);
        curr |= (WRITABLE_M0 & 0b1u) << 10u;
        curr &= ~(0b1u << 11u);
        curr |= (WRITABLE_M1 & 0b1u) << 11u;

        CTRL = curr;
    }

    /**
     * Get STAT's FIFO_EMPTY bit.
     *
     * When 1, indicates the XIP streaming FIFO is completely empty.
     */
    inline bool get_STAT_FIFO_EMPTY() volatile
    {
        return STAT & (1u << 1u);
    }

    /**
     * Get STAT's FIFO_FULL bit.
     *
     * When 1, indicates the XIP streaming FIFO is completely full.
     *                             The streaming FIFO is 2 entries deep, so the
     * full and empty flag allow its level to be ascertained.
     */
    inline bool get_STAT_FIFO_FULL() volatile
    {
        return STAT & (1u << 2u);
    }

    /**
     * Get all of STAT's bit fields.
     */
    inline void get_STAT(bool &FIFO_EMPTY, bool &FIFO_FULL) volatile
    {
        uint32_t curr = STAT;

        FIFO_EMPTY = curr & (1u << 1u);
        FIFO_FULL = curr & (1u << 2u);
    }

    /**
     * Get STREAM_ADDR's STREAM_ADDR field.
     *
     * The address of the next word to be streamed from flash to the streaming
     * FIFO. Increments automatically after each flash access. Write the
     * initial access address here before starting a streaming read.
     */
    inline uint32_t get_STREAM_ADDR() volatile
    {
        return (STREAM_ADDR >> 2u) & 0b111111111111111111111111111111u;
    }

    /**
     * Set STREAM_ADDR's STREAM_ADDR field.
     *
     * The address of the next word to be streamed from flash to the streaming
     * FIFO. Increments automatically after each flash access. Write the
     * initial access address here before starting a streaming read.
     */
    inline void set_STREAM_ADDR(uint32_t value) volatile
    {
        uint32_t curr = STREAM_ADDR;

        curr &= ~(0b111111111111111111111111111111u << 2u);
        curr |= (value & 0b111111111111111111111111111111u) << 2u;

        STREAM_ADDR = curr;
    }

    /**
     * Get STREAM_CTR's STREAM_CTR field.
     *
     * Write a nonzero value to start a streaming read. This will then
     *                             progress in the background, using flash idle
     * cycles to transfer a linear data block from flash to the streaming FIFO.
     *                             Decrements automatically (1 at a time) as
     * the stream progresses, and halts on reaching 0. Write 0 to halt an
     * in-progress stream, and discard any in-flight read, so that a new stream
     * can immediately be started (after draining the FIFO and reinitialising
     * STREAM_ADDR)
     */
    inline uint32_t get_STREAM_CTR() volatile
    {
        return STREAM_CTR & 0b1111111111111111111111u;
    }

    /**
     * Set STREAM_CTR's STREAM_CTR field.
     *
     * Write a nonzero value to start a streaming read. This will then
     *                             progress in the background, using flash idle
     * cycles to transfer a linear data block from flash to the streaming FIFO.
     *                             Decrements automatically (1 at a time) as
     * the stream progresses, and halts on reaching 0. Write 0 to halt an
     * in-progress stream, and discard any in-flight read, so that a new stream
     * can immediately be started (after draining the FIFO and reinitialising
     * STREAM_ADDR)
     */
    inline void set_STREAM_CTR(uint32_t value) volatile
    {
        uint32_t curr = STREAM_CTR;

        curr &= ~(0b1111111111111111111111u);
        curr |= (value & 0b1111111111111111111111u);

        STREAM_CTR = curr;
    }
};

static_assert(sizeof(xip_ctrl) == xip_ctrl::size);
static_assert(ifgen_struct<xip_ctrl>);

static volatile xip_ctrl *const XIP_CTRL =
    reinterpret_cast<xip_ctrl *>(0x400c8000);

}; // namespace RP2350

#endif
