/**
 * \file
 * \brief Generated by ifgen (4.7.7).
 */

#pragma once
#ifndef RP2350_STRUCTS_DMA_DEBUG_H
#define RP2350_STRUCTS_DMA_DEBUG_H

#include "../ifgen/common.h"

namespace RP2350
{

struct dma_debug
{
    /* Constant attributes. */
    static constexpr struct_id_t id = 7;    /*!< dma_debug's identifier. */
    static constexpr std::size_t size = 64; /*!< dma_debug's size in bytes. */

    /* Fields. */
    uint32_t
        CTDREQ; /*!< (read-write) Read: get channel DREQ counter (i.e. how many
                   accesses the DMA expects it can perform on the peripheral
                   without overflow/underflow. Write any value: clears the
                   counter, and cause channel to re-initiate DREQ handshake. */
    const uint32_t TCR =
        {}; /*!< (read-only) Read to get channel TRANS_COUNT reload value, i.e.
               the length of the next transfer */
    static constexpr std::size_t reserved_length = 14;
    uint32_t reserved[reserved_length];

    /* Methods. */

    /**
     * Get CTDREQ's CTDREQ field.
     */
    inline uint8_t get_CTDREQ() volatile
    {
        return CTDREQ & 0b111111u;
    }

    /**
     * Set CTDREQ's CTDREQ field.
     */
    inline void set_CTDREQ(uint8_t value) volatile
    {
        uint32_t curr = CTDREQ;

        curr &= ~(0b111111u);
        curr |= (value & 0b111111u);

        CTDREQ = curr;
    }
};

static_assert(sizeof(dma_debug) == dma_debug::size);
static_assert(ifgen_struct<dma_debug>);

static volatile dma_debug *const DMA_DEBUG_NULL =
    reinterpret_cast<dma_debug *>(0x00000000);

}; // namespace RP2350

#endif
