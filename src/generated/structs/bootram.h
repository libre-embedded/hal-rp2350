/**
 * \file
 * \brief Generated by ifgen (4.7.8).
 */

#pragma once
#ifndef RP2350_STRUCTS_BOOTRAM_H
#define RP2350_STRUCTS_BOOTRAM_H

#include "../ifgen/common.h"

namespace RP2350
{

/**
 * Additional registers mapped adjacent to the bootram, for use by the bootrom.
 */
struct bootram
{
    /* Constant attributes. */
    static constexpr struct_id_t id = 3;      /*!< bootram's identifier. */
    static constexpr std::size_t size = 2092; /*!< bootram's size in bytes. */

    /* Fields. */
    static constexpr std::size_t reserved_padding0_length = 512;
    const uint32_t reserved_padding0[reserved_padding0_length] = {};
    uint32_t WRITE_ONCE0; /*!< (read-write) This registers always ORs writes
                             into its current contents. Once a bit is set, it
                             can only be cleared by a reset. */
    uint32_t WRITE_ONCE1; /*!< (read-write) This registers always ORs writes
                             into its current contents. Once a bit is set, it
                             can only be cleared by a reset. */
    uint32_t
        BOOTLOCK_STAT; /*!< (read-write) Bootlock status register. 1=unclaimed,
                          0=claimed. These locks function identically to the
                          SIO spinlocks, but are reserved for bootrom use. */
    uint32_t BOOTLOCK0; /*!< (read-write) Read to claim and check. Write to
                           unclaim. The value returned on successful claim is 1
                           << n, and on failed claim is zero. */
    uint32_t BOOTLOCK1; /*!< (read-write) Read to claim and check. Write to
                           unclaim. The value returned on successful claim is 1
                           << n, and on failed claim is zero. */
    uint32_t BOOTLOCK2; /*!< (read-write) Read to claim and check. Write to
                           unclaim. The value returned on successful claim is 1
                           << n, and on failed claim is zero. */
    uint32_t BOOTLOCK3; /*!< (read-write) Read to claim and check. Write to
                           unclaim. The value returned on successful claim is 1
                           << n, and on failed claim is zero. */
    uint32_t BOOTLOCK4; /*!< (read-write) Read to claim and check. Write to
                           unclaim. The value returned on successful claim is 1
                           << n, and on failed claim is zero. */
    uint32_t BOOTLOCK5; /*!< (read-write) Read to claim and check. Write to
                           unclaim. The value returned on successful claim is 1
                           << n, and on failed claim is zero. */
    uint32_t BOOTLOCK6; /*!< (read-write) Read to claim and check. Write to
                           unclaim. The value returned on successful claim is 1
                           << n, and on failed claim is zero. */
    uint32_t BOOTLOCK7; /*!< (read-write) Read to claim and check. Write to
                           unclaim. The value returned on successful claim is 1
                           << n, and on failed claim is zero. */

    /* Methods. */

    /**
     * Get BOOTLOCK_STAT's BOOTLOCK_STAT field.
     */
    inline uint8_t get_BOOTLOCK_STAT() volatile
    {
        return BOOTLOCK_STAT & 0xffu;
    }

    /**
     * Set BOOTLOCK_STAT's BOOTLOCK_STAT field.
     */
    inline void set_BOOTLOCK_STAT(uint8_t value) volatile
    {
        uint32_t curr = BOOTLOCK_STAT;

        curr &= ~(0xffu);
        curr |= (value & 0xffu);

        BOOTLOCK_STAT = curr;
    }
};

static_assert(sizeof(bootram) == bootram::size);
static_assert(ifgen_struct<bootram>);

static volatile bootram *const BOOTRAM =
    reinterpret_cast<bootram *>(0x400e0000);

}; // namespace RP2350

#endif
