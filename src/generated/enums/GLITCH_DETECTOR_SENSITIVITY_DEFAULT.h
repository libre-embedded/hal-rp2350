/**
 * \file
 * \brief Generated by ifgen (4.7.7).
 */

#pragma once
#ifndef RP2350_ENUMS_GLITCH_DETECTOR_SENSITIVITY_DEFAULT_H
#define RP2350_ENUMS_GLITCH_DETECTOR_SENSITIVITY_DEFAULT_H

#include "../ifgen/common.h"
#include <cstdint>
#include <cstring>

namespace RP2350
{

enum class GLITCH_DETECTOR_SENSITIVITY_DEFAULT : uint8_t
{
    yes /*!< Use the default sensitivity configured in OTP for all detectors.
           (Any value other than DEFAULT_NO counts as YES) */
        ,
    no = 222 /*!< Do not use the default sensitivity configured in OTP. Instead
                use the value from this register. */
};
static_assert(sizeof(GLITCH_DETECTOR_SENSITIVITY_DEFAULT) == 1);

/**
 * Converts GLITCH_DETECTOR_SENSITIVITY_DEFAULT to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(GLITCH_DETECTOR_SENSITIVITY_DEFAULT instance)
{
    const char *result = "UNKNOWN GLITCH_DETECTOR_SENSITIVITY_DEFAULT";

    switch (instance)
    {
    case GLITCH_DETECTOR_SENSITIVITY_DEFAULT::yes:
        result = "yes";
        break;
    case GLITCH_DETECTOR_SENSITIVITY_DEFAULT::no:
        result = "no";
        break;
    }

    return result;
}

inline std::ostream &operator<<(std::ostream &stream,
                                GLITCH_DETECTOR_SENSITIVITY_DEFAULT instance)
{
    stream << to_string(instance);
    return stream;
}

/**
 * Converts a C string to GLITCH_DETECTOR_SENSITIVITY_DEFAULT.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data,
                        GLITCH_DETECTOR_SENSITIVITY_DEFAULT &output)
{
    bool result = false;

    if ((result = !strncmp(data, "yes", 3)))
    {
        output = GLITCH_DETECTOR_SENSITIVITY_DEFAULT::yes;
    }
    else if ((result = !strncmp(data, "no", 2)))
    {
        output = GLITCH_DETECTOR_SENSITIVITY_DEFAULT::no;
    }

    return result;
}

}; // namespace RP2350

#endif
