/**
 * \file
 * \brief Generated by ifgen (4.7.3).
 */

#pragma once
#ifndef RP2350_ENUMS_GLITCH_DETECTOR_ARM_ARM_H
#define RP2350_ENUMS_GLITCH_DETECTOR_ARM_ARM_H

#include "../ifgen/common.h"
#include <cstdint>
#include <cstring>

namespace RP2350
{

enum class GLITCH_DETECTOR_ARM_ARM : uint16_t
{
    no = 23469 /*!< Do not force the glitch detectors to be armed */,
    yes /*!< Force the glitch detectors to be armed. (Any value other than
           ARM_NO counts as YES) */
};
static_assert(sizeof(GLITCH_DETECTOR_ARM_ARM) == 2);

/**
 * Converts GLITCH_DETECTOR_ARM_ARM to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(GLITCH_DETECTOR_ARM_ARM instance)
{
    const char *result = "UNKNOWN GLITCH_DETECTOR_ARM_ARM";

    switch (instance)
    {
    case GLITCH_DETECTOR_ARM_ARM::no:
        result = "no";
        break;
    case GLITCH_DETECTOR_ARM_ARM::yes:
        result = "yes";
        break;
    }

    return result;
}

inline std::ostream &operator<<(std::ostream &stream,
                                GLITCH_DETECTOR_ARM_ARM instance)
{
    stream << to_string(instance);
    return stream;
}

/**
 * Converts a C string to GLITCH_DETECTOR_ARM_ARM.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, GLITCH_DETECTOR_ARM_ARM &output)
{
    bool result = false;

    if ((result = !strncmp(data, "no", 2)))
    {
        output = GLITCH_DETECTOR_ARM_ARM::no;
    }
    else if ((result = !strncmp(data, "yes", 3)))
    {
        output = GLITCH_DETECTOR_ARM_ARM::yes;
    }

    return result;
}

}; // namespace RP2350

#endif
