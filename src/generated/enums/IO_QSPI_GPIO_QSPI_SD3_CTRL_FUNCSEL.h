/**
 * \file
 * \brief Generated by ifgen (4.7.8).
 */

#pragma once
#ifndef RP2350_ENUMS_IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL_H
#define RP2350_ENUMS_IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL_H

#include "../ifgen/common.h"
#include <cstdint>
#include <cstring>

namespace RP2350
{

enum class IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL : uint8_t
{
    xip_sd3,
    uart0_rts = 2,
    i2c1_scl = 3,
    siob_proc_63 = 5,
    uart0_rx = 11,
    null = 31
};
static_assert(sizeof(IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL) == 1);

/**
 * Converts IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL instance)
{
    const char *result = "UNKNOWN IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL";

    switch (instance)
    {
    case IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL::xip_sd3:
        result = "xip_sd3";
        break;
    case IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL::uart0_rts:
        result = "uart0_rts";
        break;
    case IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL::i2c1_scl:
        result = "i2c1_scl";
        break;
    case IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL::siob_proc_63:
        result = "siob_proc_63";
        break;
    case IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL::uart0_rx:
        result = "uart0_rx";
        break;
    case IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL::null:
        result = "null";
        break;
    }

    return result;
}

inline std::ostream &operator<<(std::ostream &stream,
                                IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL instance)
{
    stream << to_string(instance);
    return stream;
}

/**
 * Converts a C string to IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data,
                        IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL &output)
{
    bool result = false;

    if ((result = !strncmp(data, "xip_sd3", 7)))
    {
        output = IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL::xip_sd3;
    }
    else if ((result = !strncmp(data, "uart0_rts", 9)))
    {
        output = IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL::uart0_rts;
    }
    else if ((result = !strncmp(data, "i2c1_scl", 8)))
    {
        output = IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL::i2c1_scl;
    }
    else if ((result = !strncmp(data, "siob_proc_63", 12)))
    {
        output = IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL::siob_proc_63;
    }
    else if ((result = !strncmp(data, "uart0_rx", 8)))
    {
        output = IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL::uart0_rx;
    }
    else if ((result = !strncmp(data, "null", 4)))
    {
        output = IO_QSPI_GPIO_QSPI_SD3_CTRL_FUNCSEL::null;
    }

    return result;
}

}; // namespace RP2350

#endif
